---
phase: 02-engine-core-and-base-10-migration
plan: 09
type: execute
wave: 6
depends_on: ["02-08"]
files_modified:
  - app/presets/base10/currents.ts
  - app/data/currents.ts
  - tests/presets/base10-adapter.test.ts
autonomous: true
requirements: [MIG-01]

must_haves:
  truths:
    - "The viewer's currents come from createNumogram(10) (pair hi::lo flows to zone hi - lo) joined with CURRENT_LORE by pair id; names, labels (e.g. 8−1=7 with U+2212), order and the upstream `from` convention are reproduced exactly"
    - "After the swap the full gate is green (60 DOM goldens, behaviour baseline, numeric oracle, npm run verify) and only then is the hand-authored current data deleted (D-02, D-08)"
  artifacts:
    - path: "app/presets/base10/currents.ts"
      provides: "CURRENTS: CurrentData[] derived from the engine + lore; legacyCurrentFrom(pairId)"
      exports: ["CURRENTS", "legacyCurrentFrom"]
  key_links:
    - from: "app/data/currents.ts"
      to: "app/presets/base10/currents.ts"
      via: "export { CURRENTS } from '../presets/base10/currents'"
      pattern: "presets/base10/currents"
    - from: "app/presets/base10/currents.ts"
      to: "engine/index.ts"
      via: "formatNumeral for the in-base label"
      pattern: "formatNumeral"
---

<objective>
Second data-source swap (D-02): derive `CURRENTS` from the engine joined with lore by pair id, flip the `app/data/currents.ts` seam, prove the full gate green, then delete the hand-authored current structure (from/to/label, D-08).

Purpose: MIG-01 strangler step 2. Consumers stay untouched (D-01).
Output: currents adapter, rewired seam, adapter tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-engine-core-and-base-10-migration/02-CONTEXT.md
@CLAUDE.md
@app/data/types.ts
@app/data/currents.ts
@app/presets/base10/lore.ts
@app/presets/base10/numogram.ts
@app/presets/base10/syzygies.ts
@tests/presets/base10-adapter.test.ts

<interfaces>
- `CurrentData { name: string; from: number; to: number; label: string; desc: string }`. Today's entries, in order: Surge (pair 1::8) from 8 to 7 label `8−1=7`; Hold (2::7) from 2 to 5 `7−2=5`; Sink (4::5) from 4 to 1 `5−4=1`; Warp (3::6) from 6 to 3 `6−3=3`; Plex (0::9) from 9 to 9 `9−0=9`. Key order: name, from, to, label, desc.
- Derivation rules (verified against today's data):
  - order: the Torque cycles in canonical order, each in flow order (base 10: pairs 1, 2, 4), then the Warp pair (3), then the Plex pair (0); equivalently `[...BASE10.torques.flatMap(c => Array.from(c.pairs())), ...(BASE10.warp ? [BASE10.warp.firstPair] : []), BASE10.plex.firstPair]`
  - to = `BASE10.current(p).to` (= hi - lo)
  - label = `${formatNumeral(hi, 10)}−${formatNumeral(lo, 10)}=${formatNumeral(to, 10)}` (U+2212 minus sign, in-base numerals per D-04)
  - from (upstream viewer convention, not canonical): `legacyCurrentFrom(p) = lo === 0 ? hi : even` (the pair's even member; the Plex pair folds back into its high zone 9)
  - name, desc = `CURRENT_LORE[p]`
- Frozen oracle `currents`: [{ name, pair, from, to }] for Surge, Hold, Sink, Warp, Plex.
- Consumers (NOT edited): NumogramClient.tsx (current routing keys on `c.name`), Projection.tsx, CurrentsPanel.tsx, InfoDisplay.tsx, PinnedBackground.tsx, tests/oracle/deriveBase10.ts.
</interfaces>

Rules: never edit NumogramClient.tsx, app/components/**, app/hooks/**, app/lib/** (D-01, D-03). Never regenerate goldens, baselines or fixtures. Commits end with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Never `git push`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Currents adapter and seam flip (swap commit)</name>
  <files>app/presets/base10/currents.ts, app/data/currents.ts, tests/presets/base10-adapter.test.ts</files>
  <read_first>app/data/currents.ts, app/data/types.ts, app/presets/base10/lore.ts, app/presets/base10/numogram.ts, app/presets/base10/syzygies.ts (adapter pattern), tests/presets/base10-adapter.test.ts, engine/index.ts</read_first>
  <behavior>
    - adapter CURRENTS deep-equals LEGACY_CURRENTS (hand data, still present in this task), including every label character (U+2212)
    - Object.keys of every entry is ['name', 'from', 'to', 'label', 'desc']
    - CURRENTS.map(c => ({ name: c.name, from: c.from, to: c.to })) equals the frozen oracle's currents without `pair`, in the same order
    - legacyCurrentFrom: 1 -> 8, 2 -> 2, 4 -> 4, 3 -> 6, 0 -> 9; every `to` equals BASE10.current(p).to
    - the CURRENTS exported by app/data/currents is the adapter's array (toBe)
  </behavior>
  <action>
1. `app/presets/base10/currents.ts`: `export function legacyCurrentFrom(pairId: number): number` and `export const CURRENTS: CurrentData[]` built once from the derivation rules in <interfaces> (throw `Error('base-10 lore missing for current ' + p)` if `CURRENT_LORE[p]` is absent); import `formatNumeral` from `../../../engine/index`, `CURRENT_LORE` from `./lore`, `BASE10` from `./numogram`. Doc comments state that `from` is the upstream viewer's convention kept for byte-identical rendering (Phase 4 replaces it).
2. Seam flip in `app/data/currents.ts`: rename the hand array to `export const LEGACY_CURRENTS` (kept only until Task 2) and add `export { CURRENTS } from '../presets/base10/currents'`.
3. Extend `tests/presets/base10-adapter.test.ts` FIRST with a `describe('currents')` block from <behavior> (LEGACY comparison in its own `it`).
4. `npx cross-env CCRUG_TZ=UTC vitest run --project oracle`, `npm run typecheck`, `MSYS_NO_PATHCONV=1 npm run test:swap`; commit `refactor(02-09): swap currents to the engine-derived base-10 adapter (D-02)`; then `MSYS_NO_PATHCONV=1 npm run verify` on the committed tree must be green (a failure is a defect in this plan's code: fix it, never touch goldens, baselines or fixtures).
  </action>
  <verify>
    <automated>npx cross-env CCRUG_TZ=UTC vitest run --project oracle && MSYS_NO_PATHCONV=1 npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `app/presets/base10/currents.ts` contains `legacyCurrentFrom`, `−` and `formatNumeral(`; `app/data/currents.ts` contains `export { CURRENTS } from '../presets/base10/currents'` and `LEGACY_CURRENTS`
    - `tests/presets/base10-adapter.test.ts` contains `describe('currents'` and `LEGACY_CURRENTS`
    - `MSYS_NO_PATHCONV=1 npm run verify` exits 0 on the committed swap
    - `git diff --quiet f7d6689 -- app/NumogramClient.tsx app/components app/hooks app/lib e2e/__golden__ engine/test/fixtures/base10.golden.json` exits 0; `node scripts/golden-manifest.mjs verify e2e/__behaviour__/MANIFEST.json` exits 0 and `git status --porcelain -- e2e/__behaviour__` prints nothing (the behaviour baseline is created by 02-03, so it has no f7d6689 version to diff against)
  </acceptance_criteria>
  <done>The viewer's currents come from the engine joined with lore, with the full gate green while the old data still exists.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the hand-authored current data (D-08)</name>
  <files>app/data/currents.ts, tests/presets/base10-adapter.test.ts</files>
  <read_first>app/data/currents.ts, tests/presets/base10-adapter.test.ts</read_first>
  <action>
Only after Task 1's verify was green: make `app/data/currents.ts` exactly the seam comment (`// Base-10 data seam (MIG-01): currents derived by the engine and joined with lore in app/presets/base10/currents.ts.`) plus `export { CURRENTS } from '../presets/base10/currents'`. Remove the LEGACY comparison and import from the adapter test (keep the oracle, key-order, legacyCurrentFrom and identity checks). Run `npx cross-env CCRUG_TZ=UTC vitest run --project oracle`, `npm run typecheck`, `MSYS_NO_PATHCONV=1 npm run test:swap`, `node scripts/check-repo.mjs`. Commit `refactor(02-09): delete hand-authored current data (D-08)`.
  </action>
  <verify>
    <automated>MSYS_NO_PATHCONV=1 npm run test:swap && npm run typecheck && node scripts/check-repo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "from: [0-9]|to: [0-9]|label:|LEGACY" app/data/currents.ts` prints 0 and the file has at most 3 non-empty lines
    - `grep -c "LEGACY_CURRENTS" tests/presets/base10-adapter.test.ts` prints 0
    - `npm run test:swap`, `npm run typecheck` and `node scripts/check-repo.mjs` pass
  </acceptance_criteria>
  <done>No hand-authored current structure remains; the gate is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine + lore (by pair id) -> rendered viewer | Current routing in NumogramClient keys on the current name; a mis-join changes paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-36 | Tampering | name/pair mis-join or order change | mitigate | Adapter test vs hand data (while present) and frozen oracle currents; DOM goldens (layer-currents, time-circuit states); behaviour baseline lore sweep |
| T-02-37 | Tampering | label character drift (U+2212 vs '-') | mitigate | Deep equality with the hand labels before deletion; behaviour baseline records panel labels |
| T-02-38 | Tampering | goldens regenerated to pass | mitigate | updateSnapshots none, manifests, --clean-tree in verify, acceptance diff vs f7d6689 |
</threat_model>

<verification>
- `npm run verify` green after the swap commit; `npm run test:swap` green after the deletion
</verification>

<success_criteria>
- Currents are engine-derived and lore-joined with byte-identical goldens and an unchanged behaviour baseline (MIG-01, D-02 step 2)
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-core-and-base-10-migration/02-09-SUMMARY.md`
</output>
