---
phase: 02-engine-core-and-base-10-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/behaviour-collect.ts
  - e2e/behaviour-compare.ts
  - e2e/behaviour.spec.ts
  - e2e/__behaviour__/original.json
  - e2e/__behaviour__/labyrinth.json
  - e2e/__behaviour__/ladder.json
  - e2e/__behaviour__/planetary.json
  - e2e/__behaviour__/mobile.json
  - e2e/__behaviour__/MANIFEST.json
  - tests/e2e-normalizer/behaviour-lore-coverage.test.ts
  - tests/e2e-normalizer/behaviour-compare.test.ts
  - scripts/check-repo.mjs
  - tests/repo/check-repo.test.ts
  - NOTICE
  - package.json
autonomous: true
requirements: [MIG-01]

must_haves:
  truths:
    - "A committed Playwright spec replays the D-15 behaviour script (interactive elements, panel text, hover popovers, Selection-panel detail text, URL state after each action, undo/redo, keyboard shortcuts, share) for the original, labyrinth, ladder and planetary layouts plus a 390x800 run, and compares the result with frozen JSON baselines"
    - "The baselines were captured from the current viewer before any Phase 2 change under app/ (no data-source swap, no lore move yet)"
    - "The collector records text and attribute/computed-style facts only: no bounding boxes, viewport sizes, geometry or animation lists, so a baseline captured on Windows also holds on ubuntu-latest"
    - "The frozen original.json contains every gate desc and detail, every current desc and label, every syzygy desc and every non-empty zone desc and lemurian, so a wrong lore key fails the check (lore coverage test), and the comparator demonstrably reports a single changed lore string (unit test on hand-made inputs)"
    - "The check is deterministic (three consecutive identical passes); the baselines are sha256-frozen in e2e/__behaviour__/MANIFEST.json; capture refuses to overwrite; check-repo verifies the manifest and treats e2e/__behaviour__ as a frozen LF oracle directory"
    - "npm run test:swap runs the per-swap gate (build, 60 DOM goldens, behaviour baseline, vitest)"
  artifacts:
    - path: "e2e/behaviour.spec.ts"
      provides: "Behaviour and text baseline check (D-15), compare-or-capture"
      contains: "BEHAVIOUR_CAPTURE"
    - path: "e2e/behaviour-collect.ts"
      provides: "Self-contained in-page collector (text, attributes, computed style only)"
      exports: ["collect", "PANELS"]
    - path: "e2e/behaviour-compare.ts"
      provides: "Pure Node-side helpers: diffBaseline, normalizeDeep, baselineAction"
      exports: ["diffBaseline", "normalizeDeep", "baselineAction"]
    - path: "e2e/__behaviour__/MANIFEST.json"
      provides: "sha256 freeze of the five baselines (strictDir e2e/__behaviour__)"
      contains: "behaviour-baseline"
    - path: "tests/e2e-normalizer/behaviour-lore-coverage.test.ts"
      provides: "Proof that the frozen baseline contains the lore it must guard"
      contains: "original.json"
    - path: "scripts/check-repo.mjs"
      provides: "goldens + lf + clean-tree checks cover the behaviour baseline"
      contains: "e2e/__behaviour__/MANIFEST.json"
    - path: "package.json"
      provides: "test:swap gate script"
      contains: "\"test:swap\""
  key_links:
    - from: "e2e/behaviour.spec.ts"
      to: "e2e/behaviour-compare.ts"
      via: "diffBaseline / normalizeDeep / baselineAction (the same code the unit tests cover)"
      pattern: "behaviour-compare"
    - from: "e2e/behaviour.spec.ts"
      to: "e2e/__behaviour__/*.json"
      via: "readFileSync + diffBaseline; writes only when BEHAVIOUR_CAPTURE=1 and the file is missing"
      pattern: "__behaviour__"
    - from: "scripts/check-repo.mjs"
      to: "e2e/__behaviour__/MANIFEST.json"
      via: "MANIFESTS -> verifyManifestFile"
      pattern: "MANIFESTS"
---

<objective>
Turn the Phase 2 seed (`.planning/phases/02-engine-core-and-base-10-migration/seed/inventory.mjs`) into a committed, parametrized, frozen behaviour and text baseline (D-15), captured from the current viewer BEFORE the first data-source swap and before the lore move, and wire it into the guards and the per-swap gate.

Purpose: MIG-01 requires "hover, undo and share links behave exactly as before" and no visible change, but the 60 DOM goldens only capture the projection `<svg>`: the lore text that panels, hover popovers and the Selection panel show is exactly what this phase moves, and a wrong lore key would pass every golden. This baseline closes that gap.
Output: collector, comparator helpers and spec; five frozen baselines with their manifest; lore coverage and comparator tests; check-repo registration; NOTICE note; `test:swap` script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-engine-core-and-base-10-migration/02-CONTEXT.md
@.planning/phases/02-engine-core-and-base-10-migration/seed/inventory.mjs
@.planning/notes/2026-09-25-ui-declutter-inventory.md
@e2e/golden.spec.ts
@e2e/visual-dom.ts
@tests/e2e-normalizer/visual-dom.test.ts
@playwright.config.ts
@scripts/check-repo.mjs
@scripts/golden-manifest.mjs
@NOTICE
@package.json
@CLAUDE.md

<interfaces>
- playwright.config.ts: testDir `e2e`, testMatch `/\.spec\.ts$/` (so `e2e/behaviour-collect.ts` and `e2e/behaviour-compare.ts` are not tests), projects `chromium-utc` and `chromium-ny` (1280x900), `workers: 1`, webServer `npx serve out` on 127.0.0.1:3111 (needs `npm run build` first), `use.baseURL` = `http://127.0.0.1:${PORT}`. Its snapshot machinery (`toMatchSnapshot`) writes into `e2e/__golden__/`, whose manifest is strict, so this spec must NOT use toMatchSnapshot: it compares against its own files under `e2e/__behaviour__/`.
- Vitest project `oracle` runs `tests/**/*.test.ts`; `tests/e2e-normalizer/visual-dom.test.ts` already unit-tests the pure e2e helper `e2e/visual-dom.ts` (same pattern for `e2e/behaviour-compare.ts`, which must therefore import nothing from `@playwright/test`).
- scripts/check-repo.mjs exports `MANIFESTS = ['e2e/__golden__/MANIFEST.json', 'engine/test/fixtures/MANIFEST.json']` and `LF_DIRS = ['e2e/__golden__', 'engine/test/fixtures', 'perf']`; `goldens` verifies every manifest in MANIFESTS; `lf` rejects CR bytes in LF_DIRS; `--clean-tree` rejects modified tracked files and any untracked file inside LF_DIRS.
- `node scripts/golden-manifest.mjs freeze <manifest> --set <YYYY-MM-DD-slug> --reason "<why>" [--strict-dir <dir>] <file-or-dir>` creates the manifest if missing and refuses to re-freeze a file (a later correction adds NEW files under a NEW dated set).
- NOTICE section 3 currently ends: "Frozen test fixtures derived from them (engine/test/fixtures/ and e2e/__golden__/) carry the same status for any lore text they contain."
- Lore as exported today (read-only here; the same names stay exported through Phase 2 because app/data/* become re-export seams): `GATE_LIST` (desc, detail) from app/data/gates.ts, `CURRENTS` (desc, label) from app/data/currents.ts, `SYZYGIES` (desc) from app/data/syzygies.ts, `ZONE_META` (desc, lemurian) from app/data/zones.ts.
- The seed's known defects: an absolute `createRequire('C:/Users/...')` path, a scratch base URL, a 4.6 s splash wait (the splash was removed by todo 001), fixed sleeps before readouts, and geometry/visibility decided from bounding boxes (OS- and font-dependent).
</interfaces>

Wave-1 note: plans 02-01 (engine/core, engine/index.ts, engine/test) and 02-02 (tests/bruteforce, scripts/capture-notable-bases.ts, engine/test/fixtures/derived) run in the same wave in the same working tree. This plan never modifies any tracked file outside its own files_modified, not even temporarily (there is no mutation run against app/). `npm run build` type-checks the whole repo: if it fails only on an error in a peer's file, wait for the peer's commit and rerun; never edit the peer's file. This plan runs targeted gates only (not `npm run verify`); plan 02-08 runs the first full `npm run verify` before the first swap.

Commits: stage explicit paths only (`git add <path> <path> ...`; never `git add -A`, `git add .` or `git commit -a`). End every commit message with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Never `git push`. Never regenerate a baseline or golden to make a test pass; never `-u`.

Cross-OS rule (record it in the SUMMARY): the baseline is captured on Windows and must also pass on ubuntu-latest (CI). If CI on another OS ever diverges, the remedy is to fix the collector (drop or normalise the diverging field), capture the corrected baseline into NEW files (for example a dated subdirectory of e2e/__behaviour__) from a checkout of this plan's baseline commit (the pre-swap viewer), freeze them as a NEW dated set with a written reason and point the spec at them. Never `-u`, never overwrite or re-freeze the existing set.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collector, comparator helpers and spec; capture from the current viewer; prove lore coverage</name>
  <files>e2e/behaviour-collect.ts, e2e/behaviour-compare.ts, e2e/behaviour.spec.ts, e2e/__behaviour__/original.json, e2e/__behaviour__/labyrinth.json, e2e/__behaviour__/ladder.json, e2e/__behaviour__/planetary.json, e2e/__behaviour__/mobile.json, tests/e2e-normalizer/behaviour-lore-coverage.test.ts</files>
  <read_first>.planning/phases/02-engine-core-and-base-10-migration/seed/inventory.mjs (all of it), .planning/notes/2026-09-25-ui-declutter-inventory.md (findings 1-4), e2e/golden.spec.ts (settle signal), e2e/visual-dom.ts (self-contained in-page function pattern), tests/e2e-normalizer/visual-dom.test.ts, playwright.config.ts, app/components/info/InfoDisplay.tsx (which lore fields hover shows), app/NumogramClient.tsx lines 900-960 (share flow: navigator.share, then navigator.clipboard.writeText)</read_first>
  <action>
0. Pre-flight (the viewer must be the pre-Phase-2 one): `git diff --quiet f7d6689 -- app` exits 0 and `app/presets` does not exist. If either fails, STOP and report.

1. `e2e/behaviour-collect.ts`: export `PANELS = ['Layers', 'Labels', 'Zones', 'Regions', 'Syzygies', 'Currents', 'Gates', 'Selection'] as const` and `collect(panels: readonly string[])`, the seed's in-page collector ported to typed TS and self-contained (no imports, no closures over module scope; it runs in the browser via `page.evaluate(collect, PANELS)`), changed to record text and attribute/computed-style facts only:
   - visibility of an element is decided from computed style alone: hidden if it or an ancestor has `display: none`, `visibility: hidden`, `opacity: 0`, or is a clipping ancestor (computed overflow not `visible`) whose computed `max-height` or `height` is `0px`. Never call `getBoundingClientRect`, `getClientRects` or read `offsetWidth/offsetHeight/innerWidth/innerHeight`.
   - return ONLY `{ url, svg: { viewBox, interactive }, interactive, texts, open }`: `interactive` rows are the seed's string rows (region, tag[type]{role}, accessible name, href, title, input value, flags disabled/checked/aria-expanded/aria-pressed, visible|HIDDEN from the style rule); `texts` are the seed's per-region visible texts plus popovers and other page text; `open` is the seed's computed max-height check of each panel body. Drop `viewport`, `geometry`, `offscreen`, `overflowX`, `overlays`, `pseudo`, `animations`, `shellStyleCounts` and `interactiveDetail`.
2. `e2e/behaviour-compare.ts` (pure Node-side helpers, no Playwright import):
```ts
export function normalizeDeep<T>(value: T, origin: string): T   // every string: replace /\d{4}-\d{2}-\d{2}/g with 'DATE', then every occurrence of origin with 'ORIGIN'
export function diffBaseline(expected: unknown, actual: unknown): string[]   // JSON-path of every difference ('$.stages.initial.texts.Gates: "a" != "b"'), [] when deep-equal; covers changed values, missing and extra keys, array length
export function baselineAction(fileExists: boolean, captureEnv: string | undefined): 'compare' | 'capture' | 'fail-missing'   // exists -> 'compare' (capture never overwrites); missing + '1' -> 'capture'; else 'fail-missing'
```
3. `e2e/behaviour.spec.ts` (imports `test`, `expect` from `@playwright/test`, `fs`, `path`, the collector and the helpers):
   - `test.beforeEach(({}, info) => test.skip(info.project.name !== 'chromium-utc', 'the behaviour baseline runs once; its contexts pin UTC'))`.
   - Each test creates its own context as the seed: `browser.newContext({ viewport: { width: 1440, height: 900 }, colorScheme: 'dark', locale: 'en-US', timezoneId: 'UTC', baseURL })` (baseURL from `test.info().project.use.baseURL`), plus `context.addInitScript` making the share flow deterministic on every OS: `Object.defineProperty(navigator, 'share', { value: undefined, configurable: true })` and `navigator.clipboard` replaced by `{ writeText: async t => { store = String(t) }, readText: async () => store }` (in-page variable). No clipboard permissions are requested.
   - `stable(read, quietMs = 300, timeoutMs = 10_000)`: poll every 50 ms and return once `JSON.stringify` of the value has not changed for `quietMs`; throw on timeout. Every stage snapshot and every recorded readout goes through `stable`; fixed sleeps remain only between input events where the seed needs them (at most 150 ms). Playwright `boundingBox()` may be used to position the mouse for the panel drag, but no geometry is recorded (the drag records the panel's style left/top delta, which equals the mouse delta).
   - `settle(page, layout)`: wait for `svg[viewBox^="0 0 800 "]`, the layout height (original 940, labyrinth 880, ladder 870; planetary: any), the `__reactFiber$` key on the svg, two rAF, then `stable` on the collected snapshot. No splash wait.
   - One test per layout (`original` qs '', `labyrinth`, `ladder`, `planetary` with `layout=planetary&date=2000-01-01`) with `test.setTimeout(240_000)`, running the seed's driver steps in the seed's order, with these changes: (a) DELETE the probe "real mouse click on Zones row 5 centre" (a known upstream click bug, todo 003, whose timing may vary by machine); (b) original layout only, directly after the hover section, a lore sweep: for each row of the Zones, Syzygies, Currents and Gates panels (`panel(page, name).locator('div.cursor-pointer')`, every index) `hover()` the row, wait 150 ms, record behaviour `lore sweep <Panel> row <i>` with the `stable` value of the collected `texts`, move the mouse to (700, 500) and wait for `stable`; (c) the share readout reads the stubbed clipboard.
   - One test `mobile` (390x800, original) as the seed's mobile block.
   - Output per test: `normalizeDeep({ schema: 1, layout, stages, behaviours }, new URL(baseURL).origin)`; then by `baselineAction(existsSync(file), process.env.BEHAVIOUR_CAPTURE)` with file `path.join(info.project.testDir, '__behaviour__', name + '.json')`: 'compare' parses the file and, if `diffBaseline(expected, value)` is non-empty, writes the actual text to `info.outputPath('actual-' + name + '.json')` and fails with `expect(diffs).toEqual([])`; 'capture' writes `JSON.stringify(value, null, 1) + '\n'` (LF, mkdir -p) and throws `Error('captured e2e/__behaviour__/<name>.json; rerun without BEHAVIOUR_CAPTURE to compare')`; 'fail-missing' fails with 'behaviour baseline missing'.
4. Capture on the current viewer: `npm run build`, then `BEHAVIOUR_CAPTURE=1 npx playwright test e2e/behaviour.spec.ts --project chromium-utc` (exits 1 by design: five files written). Record the capture wall time.
5. `tests/e2e-normalizer/behaviour-lore-coverage.test.ts` (oracle project): load `e2e/__behaviour__/original.json`, collect every string in it; import `GATE_LIST`, `CURRENTS`, `SYZYGIES` and `ZONE_META` from `../../app/data/*`; normalise each lore string like the collector (`replace(/\s+/g, ' ').trim()`, then the DATE mask); assert that every gate `desc` and `detail`, every current `desc` and `label`, every syzygy `desc`, and every NON-EMPTY zone `desc` and `lemurian` occurs as a substring of some recorded string (report the missing ones by id). If any is missing, extend the lore sweep for that panel (pin the row with a DOM click at `{ position: { x: 1, y: 1 } }`, record the texts, clear with the Selection panel's `clear` button), delete the five uncommitted baseline files and recapture (allowed only now, before the first commit of the baselines).
6. Commit by explicit path: `git add e2e/behaviour-collect.ts e2e/behaviour-compare.ts e2e/behaviour.spec.ts e2e/__behaviour__/original.json e2e/__behaviour__/labyrinth.json e2e/__behaviour__/ladder.json e2e/__behaviour__/planetary.json e2e/__behaviour__/mobile.json tests/e2e-normalizer/behaviour-lore-coverage.test.ts`, message `test(02-03): behaviour and text baseline of the pre-swap viewer (D-15)`.
  </action>
  <verify>
    <automated>npx playwright test e2e/behaviour.spec.ts --project chromium-utc && npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/e2e-normalizer && git diff --quiet f7d6689 -- app</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/__behaviour__/` holds exactly `original.json`, `labyrinth.json`, `ladder.json`, `planetary.json`, `mobile.json`; none contains a CR byte; each ends with `\n`
    - `grep -c "lore sweep" e2e/__behaviour__/original.json` is at least 30; `grep -l "127.0.0.1" e2e/__behaviour__/*.json` finds nothing; `grep -c "ORIGIN" e2e/__behaviour__/original.json` is at least 1
    - `grep -cE "getBoundingClientRect|getClientRects|offsetWidth|offsetHeight|innerWidth|innerHeight|getAnimations|geometry|overlays|shellStyleCounts" e2e/behaviour-collect.ts` prints 0, and the file exports `collect` and `PANELS`
    - `e2e/behaviour.spec.ts` contains `BEHAVIOUR_CAPTURE`, `chromium-utc`, `addInitScript` and `diffBaseline`, and does NOT contain `toMatchSnapshot`, `C:/Users` or `4600`
    - `tests/e2e-normalizer/behaviour-lore-coverage.test.ts` passes (every listed lore field is present in the frozen-to-be original.json)
    - `git diff --quiet f7d6689 -- app` exits 0 at every point of this task (no app file was modified, even temporarily); the commit lists only the nine paths above
  </acceptance_criteria>
  <done>An OS-independent behaviour and text baseline of the untouched viewer exists for four layouts and mobile, and it demonstrably contains the lore it has to guard.</done>
</task>

<task type="auto">
  <name>Task 2: Prove determinism, freeze the baseline, register it in check-repo, note it in NOTICE, add the test:swap gate</name>
  <files>e2e/__behaviour__/MANIFEST.json, scripts/check-repo.mjs, tests/repo/check-repo.test.ts, NOTICE, package.json</files>
  <read_first>scripts/check-repo.mjs, tests/repo/check-repo.test.ts, scripts/golden-manifest.mjs, NOTICE, package.json, e2e/behaviour.spec.ts</read_first>
  <action>
1. Determinism before freezing: `npm run build`, then `npx playwright test e2e/behaviour.spec.ts --project chromium-utc --repeat-each 3` must pass 15/15, and `npx playwright test e2e/behaviour.spec.ts` must show 5 passed and 5 skipped (chromium-ny). Record in the SUMMARY the measured wall-clock of one full single pass of the behaviour spec and of the 3x run (target: under 4 minutes per pass). If any run differs, fix the collector or the stability waits (never the baseline content by hand), delete the five committed-but-not-yet-frozen baseline files, recapture them as in Task 1 (capture never overwrites, so deletion comes first), rerun the lore coverage test, commit them by explicit path, and repeat the 3x run. Nothing is frozen before step 2.
2. Freeze: `node scripts/golden-manifest.mjs freeze e2e/__behaviour__/MANIFEST.json --set <YYYY-MM-DD>-behaviour-baseline --strict-dir e2e/__behaviour__ --reason "Behaviour and text baseline (D-15) of the viewer before any Phase 2 data-source swap or lore move: 4 layouts + 390x800, panel text, hover popovers, lore sweep, Selection panel, URL after each action, undo/redo, shortcuts, share (stubbed clipboard); text and attribute/computed-style facts only; captured from HEAD with app/ identical to f7d6689" e2e/__behaviour__` (execution date, UTC). `node scripts/golden-manifest.mjs verify e2e/__behaviour__/MANIFEST.json` exits 0.
3. `scripts/check-repo.mjs`: `export const MANIFESTS = ['e2e/__golden__/MANIFEST.json', 'engine/test/fixtures/MANIFEST.json', 'e2e/__behaviour__/MANIFEST.json']` and `export const LF_DIRS = ['e2e/__golden__', 'engine/test/fixtures', 'perf', 'e2e/__behaviour__']`. Change nothing else.
4. `tests/repo/check-repo.test.ts`: import `MANIFESTS` and `LF_DIRS` and add `it('covers the behaviour baseline: its manifest is verified and its directory is a frozen LF oracle dir', ...)` asserting both contain the new entries.
5. NOTICE section 3: replace the sentence "Frozen test fixtures derived from them (engine/test/fixtures/ and e2e/__golden__/) carry the same status for any lore text they contain." with "Frozen test fixtures derived from them (engine/test/fixtures/, e2e/__golden__/ and the behaviour baseline in e2e/__behaviour__/) carry the same status for any lore text they contain." (re-wrap to the file's ~76-column width; change nothing else; no upstream branding).
6. package.json scripts: add `"test:swap": "npm run build && playwright test e2e/golden.spec.ts e2e/behaviour.spec.ts && npm run test"`. Leave `verify` unchanged (its `test:e2e` step already runs every spec, including the behaviour baseline).
7. Run `node scripts/check-repo.mjs`, `npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/repo`, `npx playwright test e2e/golden.spec.ts e2e/behaviour.spec.ts` (60 goldens + 5 behaviour pass, 5 skipped). Commit by explicit path: `git add e2e/__behaviour__/MANIFEST.json scripts/check-repo.mjs tests/repo/check-repo.test.ts NOTICE package.json`, message `test(02-03): freeze the behaviour baseline and add the per-swap gate`.
  </action>
  <verify>
    <automated>node scripts/check-repo.mjs && npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/repo && node scripts/golden-manifest.mjs verify e2e/__behaviour__/MANIFEST.json</automated>
  </verify>
  <acceptance_criteria>
    - The repeat-each 3 run passed 15/15 and the SUMMARY records the wall-clock of one pass and of the 3x run
    - `e2e/__behaviour__/MANIFEST.json` contains `"strictDir": "e2e/__behaviour__"`, a set name ending `-behaviour-baseline`, and 5 file entries
    - `scripts/check-repo.mjs` contains `'e2e/__behaviour__/MANIFEST.json'` inside `MANIFESTS` and `'e2e/__behaviour__'` inside `LF_DIRS`; `git diff f7d6689 -- scripts/check-repo.mjs` changes only those two lines
    - `NOTICE` contains `e2e/__behaviour__/` and still contains `lumpenspace/ccru` and the five `app/data/*.ts` lines
    - `package.json` contains `"test:swap": "npm run build && playwright test e2e/golden.spec.ts e2e/behaviour.spec.ts && npm run test"`
    - `node scripts/check-repo.mjs` prints `check-repo: OK`; `git diff --quiet f7d6689 -- e2e/__golden__ engine/test/fixtures/MANIFEST.json engine/test/fixtures/base10.golden.json app` exits 0; the commit lists only the five paths above
  </acceptance_criteria>
  <done>The baseline is proven deterministic, frozen and guarded like the other oracles, and every later swap has a one-command gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the baseline check fails on drift (comparator and capture-guard unit tests)</name>
  <files>tests/e2e-normalizer/behaviour-compare.test.ts</files>
  <read_first>e2e/behaviour-compare.ts, tests/e2e-normalizer/visual-dom.test.ts, e2e/__behaviour__/original.json</read_first>
  <behavior>
    - diffBaseline(x, structuredClone(x)) is [] for a hand-made baseline x shaped like { schema: 1, layout: 'original', stages: { initial: { texts: { Gates: '...' } } }, behaviours: [{ name, value }] }
    - changing ONE lore-like string inside x.stages.initial.texts.Gates makes diffBaseline return exactly one entry whose path contains 'stages.initial.texts.Gates'
    - a removed key, an added key and a changed array length are each reported (one entry each)
    - taking the real frozen original.json and changing one character of one recorded lore-sweep string makes diffBaseline non-empty (the real baseline is not vacuous)
    - normalizeDeep masks '2000-01-01' to 'DATE' and 'http://127.0.0.1:3111' to 'ORIGIN' in nested strings and leaves numbers and booleans alone
    - baselineAction(true, '1') === 'compare' (capture never overwrites), baselineAction(false, '1') === 'capture', baselineAction(false, undefined) === 'fail-missing'
  </behavior>
  <action>
This replaces a mutation run against app/data: editing tracked app files, even temporarily, in the shared tree while wave-1 peers run whole-repo checks is not allowed. Write `tests/e2e-normalizer/behaviour-compare.test.ts` (oracle project) from <behavior>; hand-made inputs are inline literals (no lore text copied into the test: use placeholder strings such as 'lore A'); the real-baseline case reads `e2e/__behaviour__/original.json` (read-only) and mutates an in-memory copy. Run `npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/e2e-normalizer` and the America/New_York run. Commit by explicit path: `git add tests/e2e-normalizer/behaviour-compare.test.ts`, message `test(02-03): the behaviour comparator reports drift and capture never overwrites`.
  </action>
  <verify>
    <automated>npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/e2e-normalizer && npx cross-env CCRUG_TZ=America/New_York vitest run --project oracle tests/e2e-normalizer</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e-normalizer/behaviour-compare.test.ts` contains `diffBaseline`, `normalizeDeep`, `baselineAction`, `'fail-missing'` and `original.json`
    - Both timezone runs of tests/e2e-normalizer pass
    - `git status --porcelain -- app e2e/__behaviour__` prints nothing (no tracked app file or baseline touched); the commit lists only the test file
  </acceptance_criteria>
  <done>The check provably fails when a single recorded lore string changes, and capture can never overwrite a frozen baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> page (init script) | The spec injects stubs into the page; they must not change what is being measured |
| capture mode -> tracked baseline | A test writes files that become a frozen oracle |
| developer machine (clipboard, share sheet) -> test | Real OS integrations must not be touched or leak into baselines |
| Windows capture -> Linux CI | The frozen baseline must not depend on OS fonts or layout |
| this plan -> shared working tree | Wave-1 peers build, type-check and commit in the same tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | e2e/__behaviour__/*.json regenerated to make a swap pass | mitigate | baselineAction never overwrites; sha256 strict manifest; check-repo `goldens`, `lf` and `--clean-tree` cover the directory; corrections only as a new dated set |
| T-02-11 | Information Disclosure | host/port or local dates in baselines | mitigate | normalizeDeep masks origin to ORIGIN and dates to DATE (unit-tested); acceptance greps for 127.0.0.1 |
| T-02-12 | Information Disclosure | lore text inside the baselines | mitigate | NOTICE section 3 names e2e/__behaviour__/ as carrying the lore's status |
| T-02-13 | Tampering | real clipboard / Web Share on the developer machine | mitigate | Init-script stubs; no clipboard permissions granted |
| T-02-14 | Repudiation | flaky or OS-dependent baseline accepted as truth | mitigate | Text/attribute/computed-style fields only (grep acceptance), stability polling, 3 consecutive identical passes before freezing, documented new-set remedy |
| T-02-15 | Tampering | peers disturbed by edits in the shared tree | mitigate | No mutation run; no tracked file outside files_modified is ever edited; explicit-path staging only |
| T-02-15b | Denial of Service | CI time budget (30 min job) | accept | One project only, ~4 min target, measured wall-clock recorded in the SUMMARY |
</threat_model>

<verification>
- `npx playwright test e2e/golden.spec.ts e2e/behaviour.spec.ts` passes (60 goldens + 5 behaviour, 5 skipped)
- `npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/e2e-normalizer tests/repo` passes
- `node scripts/check-repo.mjs` passes; `git diff --quiet f7d6689 -- app e2e/__golden__` exits 0
</verification>

<success_criteria>
- The D-15 baseline exists, is frozen and guarded, was captured before any Phase 2 app change, contains the lore it guards, and its comparator provably fails on drift
- `npm run test:swap` is ready for plans 02-08..02-12
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-core-and-base-10-migration/02-03-SUMMARY.md`
</output>
