---
phase: 02-engine-core-and-base-10-migration
plan: 07
type: execute
wave: 4
depends_on: ["02-03", "02-05"]
files_modified:
  - app/presets/base10/lore.ts
  - tests/presets/base10-lore.test.ts
  - app/data/zones.ts
  - app/data/syzygies.ts
  - app/data/currents.ts
  - app/data/gates.ts
  - app/data/demons.ts
  - NOTICE
  - scripts/check-repo.mjs
  - tests/repo/check-repo.test.ts
autonomous: true
requirements: [MIG-01]

must_haves:
  truths:
    - "All CCRU-derived base-10 lore (zone colours, particles, planet symbols, zone meta, syzygy demon names and descriptions, current names and descriptions, gate descriptions and details, the 45 demon names) lives in one typed module app/presets/base10/lore.ts, text unchanged, keyed by numeric ids (zone, pair id = low zone, gate origin zone, mesh number), with the licence header on line 1"
    - "A test proves every engine base-10 id (10 zones, 5 pairs, 10 gates, 45 mesh numbers from createNumogram(10)) has lore and no lore entry is orphaned, and that the lore names match the frozen oracle"
    - "The five app/data files no longer contain lore text or the lore header; they keep their (still hand-authored) structure and read text by id from lore.ts until each source is swapped"
    - "NOTICE section 3 names app/presets/base10/lore.ts, scripts/check-repo.mjs LORE_FILES is ['app/presets/base10/lore.ts'], its tests match, and the derived notable-bases manifest is verified by check-repo, all in the same commit (D-16)"
    - "The viewer is unchanged: 60 DOM goldens, the behaviour baseline and the numeric oracle pass"
  artifacts:
    - path: "app/presets/base10/lore.ts"
      provides: "ZONE_CLR, ZONE_PARTICLE, PLANET_SYMBOL, ZONE_META, SYZYGY_LORE, CURRENT_LORE, GATE_LORE, DEMON_NAMES"
      contains: "// CCRU-derived lore. Not covered by the MIT license; see NOTICE."
    - path: "tests/presets/base10-lore.test.ts"
      provides: "Lore coverage vs engine ids, no orphans, names vs frozen oracle"
      contains: "createNumogram(10)"
    - path: "scripts/check-repo.mjs"
      provides: "LORE_FILES names the single lore file; MANIFESTS includes the derived fixture manifest"
      contains: "app/presets/base10/lore.ts"
    - path: "NOTICE"
      provides: "Section 3 names the single lore file"
      contains: "app/presets/base10/lore.ts"
  key_links:
    - from: "app/data/*.ts"
      to: "app/presets/base10/lore.ts"
      via: "relative imports of the lore tables by numeric id"
      pattern: "presets/base10/lore"
    - from: "tests/presets/base10-lore.test.ts"
      to: "engine/index.ts"
      via: "createNumogram(10) supplies the ids"
      pattern: "engine/index"
---

<objective>
Consolidate the base-10 lore (D-06, D-07) into one typed, licence-marked module keyed by numeric ids, and update NOTICE, the lore header and the check-repo guard plus its tests in the same change (D-16), with no visible change to the viewer.

Purpose: MIG-01 joins engine output with lore "by id"; the swaps (02-08..02-12) need the lore already keyed that way. Doing the move before the swaps keeps each swap a pure structure change.
Output: `app/presets/base10/lore.ts`, lore coverage test, rewired `app/data/*`, NOTICE, guard + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-engine-core-and-base-10-migration/02-CONTEXT.md
@CLAUDE.md
@app/data/types.ts
@app/data/zones.ts
@app/data/syzygies.ts
@app/data/currents.ts
@app/data/gates.ts
@app/data/demons.ts
@NOTICE
@scripts/check-repo.mjs
@tests/repo/check-repo.test.ts
@engine/index.ts

<interfaces>
Current lore-bearing exports (every file starts with the header `// CCRU-derived lore. Not covered by the MIT license; see NOTICE.`):
- app/data/zones.ts: `ZONE_CLR`, `ZONE_REGION` (structure, stays until 02-11), `ZONE_PARTICLE`, `PLANET_SYMBOL`, `ZONE_META: Record<number, ZoneMeta>` (all keyed by zone 0..9)
- app/data/syzygies.ts: `SYZYGIES: SyzygyData[]` `{ a, b, demon, desc }`, order (4,5), (3,6), (2,7), (1,8), (0,9)
- app/data/currents.ts: `CURRENTS: CurrentData[]` `{ name, from, to, label, desc }`: Surge from 8 (pair 1), Hold 2 (pair 2), Sink 4 (pair 4), Warp 6 (pair 3), Plex 9 (pair 0)
- app/data/gates.ts: `GATE_LIST: GateData[]` `{ name, from, to, cum, desc, detail }` for from = 0..9
- app/data/demons.ts: `DEMON_NAMES: Record<string, string>` keyed `'a:b'` (no consumer outside the file), `TC`, `ALL_DEMONS` (import-time builder, stays until 02-12)
Consumers import from `app/data/*` and are NOT edited (D-01, D-03): NumogramClient.tsx, app/components/** (incl. projection/**), tests/oracle/deriveBase10.ts.
Guard: scripts/check-repo.mjs exports `LORE_FILES` (five app/data paths), `LORE_HEADER`, `MANIFESTS` (after 02-03: three entries), `missingNoticeEntries(text)` (LORE_FILES + 'lumpenspace/ccru'), `loreProblems(read)` (header on line 1 of each lore file). tests/repo/check-repo.test.ts hard-codes "five lore files", `toHaveLength(6)`, `'app/data/gates.ts'` replacements and a 3-problem loreProblems case.
Engine (02-04/02-05): `createNumogram(10)` with `zoneCount`, `pairCount`, `pair(id).hi`, `demons.count`, `demons.meshOf(a, b)`.
Mesh keys: mesh(a, b) = a(a - 1)/2 + b, e.g. '1:0' -> 0, '5:4' -> 14, '6:3' -> 18, '9:0' -> 36, '9:8' -> 44.
</interfaces>

Rules: the lore text is third-party (CCRU-derived) and is moved, never edited: no rewording, no escape changes that alter the runtime string (keep the existing `—`-style escapes verbatim). Do not paste lore into planning docs or commit messages. No upstream branding. Do not edit NumogramClient.tsx, app/components/** or app/lib/** (D-01, D-03).

Wave-4 note: plan 02-06 (engine/core/unrank.ts, demons.ts, types.ts, engine/test/demons.unrank.test.ts, engine/test/ceiling.test.ts) runs in the same wave in the same tree. Do not edit its files, and use the targeted gate below instead of `npm run verify`/`npm run test:swap` (they run the engine tests the peer is changing). If a check fails only in a peer's file, wait for the peer's commit and rerun.

Commits: stage explicit paths only (`git add <path> <path> ...`, never `git add -A`, `git add .` or `git commit -a`; wave peers work in the same tree). End every commit message with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Never `git push`. Never regenerate goldens or baselines.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the single lore module and prove it equal to the old lore</name>
  <files>app/presets/base10/lore.ts, tests/presets/base10-lore.test.ts</files>
  <read_first>app/data/zones.ts, app/data/syzygies.ts, app/data/currents.ts, app/data/gates.ts, app/data/demons.ts, app/data/types.ts, scripts/check-repo.mjs (LORE_HEADER), engine/index.ts</read_first>
  <behavior>
    - lore.ts line 1 equals LORE_HEADER exactly
    - ZONE_CLR, ZONE_PARTICLE, PLANET_SYMBOL, ZONE_META deep-equal the current exports of app/data/zones.ts
    - for each old SYZYGIES entry s: SYZYGY_LORE[min(s.a, s.b)] deep-equals { demon: s.demon, desc: s.desc }
    - for each old CURRENTS entry c: CURRENT_LORE[pair id containing c.from] deep-equals { name: c.name, desc: c.desc }
    - for each old GATE_LIST entry g: GATE_LORE[g.from] deep-equals { desc: g.desc, detail: g.detail }
    - for each old DEMON_NAMES key 'a:b': DEMON_NAMES[a(a-1)/2 + b] equals its name, and both tables have 45 entries
    - coverage: the numeric key sets are exactly the engine ids of createNumogram(10): zones 0..9 (ZONE_CLR, ZONE_PARTICLE, PLANET_SYMBOL, ZONE_META, GATE_LORE), pair ids 0..4 (SYZYGY_LORE, CURRENT_LORE), mesh 0..44 (DEMON_NAMES); every key is a canonical non-negative integer string; non-empty strings are required only for the string-valued tables (ZONE_CLR, ZONE_PARTICLE, PLANET_SYMBOL, DEMON_NAMES) and for the `name`, `desc`, `detail` and `demon` fields of SYZYGY_LORE, CURRENT_LORE and GATE_LORE; ZoneMeta string fields may legitimately be empty (today zone 0 `door` and zone 2 `spinal` are empty) and the test must not require otherwise (the lore text is never edited)
    - consistency: SYZYGY_LORE[p].demon === DEMON_NAMES[g.demons.meshOf(g.pair(p).hi, p)] for every pair p (a syzygy's demon is its syzygetic demon)
    - frozen oracle: for m = 0..44 DEMON_NAMES[m] equals golden.demons[m].name, and CURRENT_LORE[golden.currents[i].pair[0]].name equals golden.currents[i].name
  </behavior>
  <action>
1. Create `app/presets/base10/lore.ts`. Line 1 exactly `// CCRU-derived lore. Not covered by the MIT license; see NOTICE.`; lines 2-3 comment: "The single CCRU-derived lore module named in NOTICE section 3 (MIG-01, D-06, D-07); text unchanged from the upstream viewer, keyed by numeric engine ids: zone 0..9, pair id = low zone 0..4, gate origin zone 0..9, demon mesh number a(a-1)/2 + b." (line 1 stays exactly the LORE_HEADER the guard checks) Exports (typed module, D-07):
```ts
import type { ZoneMeta } from '../../data/types'
export interface SyzygyLore { readonly demon: string; readonly desc: string }
export interface CurrentLore { readonly name: string; readonly desc: string }
export interface GateLore { readonly desc: string; readonly detail: string }
export const ZONE_CLR: Record<number, string>        // moved verbatim from app/data/zones.ts
export const ZONE_PARTICLE: Record<number, string>   // moved verbatim
export const PLANET_SYMBOL: Record<number, string>   // moved verbatim
export const ZONE_META: Record<number, ZoneMeta>     // moved verbatim
export const SYZYGY_LORE: Record<number, SyzygyLore> // keyed by pair id (low zone)
export const CURRENT_LORE: Record<number, CurrentLore> // keyed by pair id: 0 Plex, 1 Surge, 2 Hold, 3 Warp, 4 Sink
export const GATE_LORE: Record<number, GateLore>     // keyed by gate origin zone
export const DEMON_NAMES: Record<number, string>     // keyed by mesh number
```
Copy the strings character for character (keep escapes such as `—`, `☉`, `−` exactly as written). Re-key only (never reword).
2. Create `tests/presets/base10-lore.test.ts` (oracle project) FIRST with every <behavior> bullet: the old exports are imported from `../../app/data/*` under aliases (for example `import { DEMON_NAMES as OLD_DEMON_NAMES } from '../../app/data/demons'`), the engine from `../../engine/index`, the frozen oracle via `readFileSync(fileURLToPath(new URL('../../engine/test/fixtures/base10.golden.json', import.meta.url)))`, `LORE_HEADER` from `../../scripts/check-repo.mjs`. Group the old-vs-new comparisons in a `describe('equivalence with the pre-move lore (removed in Task 2)')` block.
Run `npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/presets` and `npx tsc --noEmit --incremental false`. Commit `refactor(02-07): single base-10 lore module keyed by engine ids (D-06, D-07)`.
  </action>
  <verify>
    <automated>npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/presets && npx tsc --noEmit --incremental false</automated>
  </verify>
  <acceptance_criteria>
    - `head -n 1 app/presets/base10/lore.ts` prints exactly `// CCRU-derived lore. Not covered by the MIT license; see NOTICE.`
    - `app/presets/base10/lore.ts` contains `export const SYZYGY_LORE`, `export const CURRENT_LORE`, `export const GATE_LORE`, `export const DEMON_NAMES: Record<number, string>` and `export const ZONE_META`
    - `tests/presets/base10-lore.test.ts` contains `createNumogram(10)`, `meshOf`, `base10.golden.json` and `LORE_HEADER`
    - The oracle-project run of tests/presets passes (old and new lore are equal while both exist); root tsc exits 0
    - The Task 1 commit touches exactly two files: `git log --grep='single base-10 lore module' --name-only --format= | sort -u` prints only `app/presets/base10/lore.ts` and `tests/presets/base10-lore.test.ts` (app/data is not edited in this task)
  </acceptance_criteria>
  <done>One lore module exists, keyed by numeric ids, provably equal to the old lore and covering exactly the engine's base-10 ids.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire app/data to the lore module and update NOTICE, header and guard in the same commit (D-16)</name>
  <files>app/data/zones.ts, app/data/syzygies.ts, app/data/currents.ts, app/data/gates.ts, app/data/demons.ts, tests/presets/base10-lore.test.ts, NOTICE, scripts/check-repo.mjs, tests/repo/check-repo.test.ts</files>
  <read_first>app/data/zones.ts, app/data/syzygies.ts, app/data/currents.ts, app/data/gates.ts, app/data/demons.ts, app/presets/base10/lore.ts, NOTICE, scripts/check-repo.mjs, tests/repo/check-repo.test.ts, tests/presets/base10-lore.test.ts</read_first>
  <action>
1. Rewrite the five files so they hold NO lore text and NO lore header, keep their current exports and hand structure, and read text by id. Each starts with the comment `// Base-10 data seam (MIG-01). Lore text lives in app/presets/base10/lore.ts (CCRU-derived, see NOTICE).` Exactly:
   - `app/data/zones.ts`: `export { ZONE_CLR, ZONE_PARTICLE, PLANET_SYMBOL, ZONE_META } from '../presets/base10/lore'` and the unchanged `export const ZONE_REGION: Record<number, Region> = { ... }` (import type Region).
   - `app/data/syzygies.ts`: same `SYZYGIES` array in the same order, each entry `{ a, b, demon: SYZYGY_LORE[lo].demon, desc: SYZYGY_LORE[lo].desc }` with the entry's literal a, b and lo = min(a, b); keys in the order a, b, demon, desc.
   - `app/data/currents.ts`: same order, each `{ name: CURRENT_LORE[p].name, from, to, label, desc: CURRENT_LORE[p].desc }` with the existing literal from, to and label (keep `'−'` escapes), p the pair id (Surge 1, Hold 2, Sink 4, Warp 3, Plex 0).
   - `app/data/gates.ts`: same order, each `{ name, from, to, cum, desc: GATE_LORE[from].desc, detail: GATE_LORE[from].detail }` with the existing literal name, from, to, cum.
   - `app/data/demons.ts`: remove the string-keyed `DEMON_NAMES`; keep `TC` and the `ALL_DEMONS` loop, whose name lookup becomes `DEMON_NAMES[i * (i - 1) / 2 + j] || '?'` with `DEMON_NAMES` imported from the lore module.
2. `tests/presets/base10-lore.test.ts`: delete the `equivalence with the pre-move lore` block (now tautological); keep header, coverage, consistency and frozen-oracle checks.
3. NOTICE: in section 1 add the line `   app/presets/base10/ except lore.ts (the base-10 engine adapter),` after the line naming app/components/navigation/LegacyQueryRedirect.tsx. Replace section 3's body with:
```
   The base-10 zone, syzygy, current, gate and demon names and descriptions
   in the following file follow the writings of the Cybernetic Culture
   Research Unit (CCRU). They are third-party lore, are not covered by the
   MIT license, and are kept unchanged:
     app/presets/base10/lore.ts
   Until Phase 2 they lived in app/data/zones.ts, gates.ts, currents.ts,
   syzygies.ts and demons.ts, which no longer hold lore text. Frozen test
   fixtures derived from them (engine/test/fixtures/, e2e/__golden__/ and
   the behaviour baseline in e2e/__behaviour__/) carry the same status for
   any lore text they contain.
```
4. `scripts/check-repo.mjs`: `export const LORE_FILES = ['app/presets/base10/lore.ts']`; add `'engine/test/fixtures/derived/MANIFEST.json'` to `MANIFESTS` (registers the fixture frozen in 02-02); update the two JSDoc comments that say "five" lore files to "the CCRU-derived lore file". No other logic change.
5. `tests/repo/check-repo.test.ts`: `missingNoticeEntries` tests expect `LORE_FILES.length + 1` entries for an empty NOTICE and drop `'app/presets/base10/lore.ts'` instead of `'app/data/gates.ts'`; `loreProblems` test covers three single-file cases (header missing, header on line 2, unreadable), each yielding exactly one problem naming `app/presets/base10/lore.ts`; add `expect(LORE_FILES).toEqual(['app/presets/base10/lore.ts'])` and `expect(MANIFESTS).toContain('engine/test/fixtures/derived/MANIFEST.json')`.
6. Targeted gate (all must pass before committing): `npm run build`; `npx playwright test e2e/golden.spec.ts e2e/behaviour.spec.ts` (60 goldens + 5 behaviour, none regenerated); `npx cross-env CCRUG_TZ=UTC vitest run --project oracle` and the America/New_York run; `npx tsc --noEmit --incremental false`; `npm run lint`; `node scripts/check-repo.mjs`. Commit ALL files of this task in ONE commit (D-16): `refactor(02-07): lore consolidation with NOTICE and check-repo updated together (D-16)`. Then `node scripts/check-repo.mjs --clean-tree` passes.
  </action>
  <verify>
    <automated>npm run build && npx playwright test e2e/golden.spec.ts e2e/behaviour.spec.ts && npx cross-env CCRUG_TZ=UTC vitest run --project oracle && node scripts/check-repo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "CCRU-derived lore" app` prints exactly `app/presets/base10/lore.ts`
    - `grep -cE "desc: '|detail: '|demon: '" app/data/syzygies.ts app/data/currents.ts app/data/gates.ts` prints 0 for each file; `grep -c "ZONE_META: Record" app/data/zones.ts` prints 0
    - `NOTICE` contains `app/presets/base10/lore.ts` and `e2e/__behaviour__/` and `lumpenspace/ccru`; `scripts/check-repo.mjs` contains `export const LORE_FILES = ['app/presets/base10/lore.ts']` and `'engine/test/fixtures/derived/MANIFEST.json'`
    - `git diff --quiet f7d6689 -- app/NumogramClient.tsx app/components app/lib app/hooks` exits 0 (no consumer edited)
    - The targeted gate passes; `git diff --quiet f7d6689 -- e2e/__golden__ engine/test/fixtures/base10.golden.json engine/test/fixtures/MANIFEST.json` exits 0; the lore move is one commit containing app/data/*, NOTICE, scripts/check-repo.mjs and tests/repo/check-repo.test.ts
  </acceptance_criteria>
  <done>Lore lives in one marked module; app/data holds structure only; NOTICE, header and guard agree and were changed together; the viewer is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party CCRU lore -> repository licensing | Lore must stay marked and excluded from MIT wherever it lives |
| guard (check-repo) -> repo state | The guard must describe the new layout or verify goes red / stops protecting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-28 | Information Disclosure | lore losing its licence marking | mitigate | Header on line 1 of lore.ts, NOTICE section 3 names it, check-repo `lore` + `notice` checks updated in the same commit |
| T-02-29 | Tampering | lore text altered during the move | mitigate | Task 1 equivalence test against the old exports while both exist; frozen oracle names; behaviour baseline (lore sweep) and DOM goldens after the rewire |
| T-02-30 | Tampering | wrong id keying (orphan or missing lore) | mitigate | Coverage test against createNumogram(10) ids; syzygy/demon name consistency |
| T-02-31 | Repudiation | guard silently weakened | mitigate | check-repo tests assert the exact LORE_FILES and the new manifest; `node scripts/check-repo.mjs` must pass |
</threat_model>

<verification>
- Targeted gate green (build, 60 goldens, behaviour baseline, oracle project in two timezones, tsc, lint, check-repo, check-repo --clean-tree)
- No consumer file changed since f7d6689; frozen oracles untouched
</verification>

<success_criteria>
- D-06, D-07 and D-16 done: one typed lore file keyed by numeric ids, NOTICE and guard updated together, coverage proven
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-core-and-base-10-migration/02-07-SUMMARY.md`
</output>
