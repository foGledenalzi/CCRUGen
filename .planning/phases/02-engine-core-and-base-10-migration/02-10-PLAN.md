---
phase: 02-engine-core-and-base-10-migration
plan: 10
type: execute
wave: 7
depends_on: ["02-09"]
files_modified:
  - app/presets/base10/gates.ts
  - app/data/gates.ts
  - tests/presets/base10-adapter.test.ts
autonomous: true
requirements: [MIG-01]

must_haves:
  truths:
    - "The viewer's gates come from createNumogram(10) (zone k flows to the in-base digital root of T(k), T(0) -> 0; all n gates including Gt-00) joined with GATE_LORE by origin zone, and every gate name is written by the engine's own-base formatter (formatGateName: Gt-00 ... Gt-45 at base 10, D-04/D-05)"
    - "Gt-15 is 5 -> 6 and Gt-03 is 2 -> 3 in the viewer's data (named regressions)"
    - "After the swap the full gate is green and only then is the hand-authored gate data deleted (D-02, D-08)"
  artifacts:
    - path: "app/presets/base10/gates.ts"
      provides: "GATE_LIST: GateData[] derived from the engine + lore"
      exports: ["GATE_LIST"]
  key_links:
    - from: "app/data/gates.ts"
      to: "app/presets/base10/gates.ts"
      via: "export { GATE_LIST } from '../presets/base10/gates'"
      pattern: "presets/base10/gates"
    - from: "app/presets/base10/gates.ts"
      to: "engine/index.ts"
      via: "formatGateName(cumulation, 10)"
      pattern: "formatGateName\\("
---

<objective>
Third data-source swap (D-02): derive `GATE_LIST` from the engine's gates (in-base digital root of T(k)) with names produced by the engine's own-base numeral formatter (D-04, D-05 "the base-10 adapter's Gt-NN names use it, so the rule is exercised from day one"), joined with gate lore by origin zone; flip the seam, prove the full gate green, then delete the hand-authored gate structure (name/from/to/cum, D-08).

Purpose: MIG-01 strangler step 3.
Output: gates adapter, rewired seam, adapter tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-engine-core-and-base-10-migration/02-CONTEXT.md
@CLAUDE.md
@app/data/types.ts
@app/data/gates.ts
@app/presets/base10/lore.ts
@app/presets/base10/numogram.ts
@tests/presets/base10-adapter.test.ts

<interfaces>
- `GateData { name: string; from: number; to: number; cum: number; desc: string; detail: string }`; key order name, from, to, cum, desc, detail; order: from = 0..9. Today: Gt-00 0->0 (0), Gt-01 1->1 (1), Gt-03 2->3 (3), Gt-06 3->6 (6), Gt-10 4->1 (10), Gt-15 5->6 (15), Gt-21 6->3 (21), Gt-28 7->1 (28), Gt-36 8->9 (36), Gt-45 9->9 (45).
- Engine: `BASE10.gate(k)` -> `{ from, to, cumulation }` with to = `T === 0 ? 0 : ((T - 1) % (n - 1)) + 1`; `formatGateName(cumulation, 10)` -> 'Gt-' + two in-base digits.
- `GATE_LORE: Record<number, { desc, detail }>` keyed by origin zone.
- Gate 0 -> 0 policy (locked): the engine emits all n gates; the base-10 viewer keeps drawing what it draws today, so the adapter keeps all 10 entries including Gt-00.
- Consumers (NOT edited): NumogramClient.tsx (gate routing keys on `g.name`), Projection.tsx, GatesPanel.tsx, InfoDisplay.tsx, tests/oracle/deriveBase10.ts.
</interfaces>

Rules: never edit NumogramClient.tsx, app/components/**, app/hooks/**, app/lib/** (D-01, D-03). Never regenerate goldens, baselines or fixtures. Commits end with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Never `git push`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gates adapter with own-base names and seam flip (swap commit)</name>
  <files>app/presets/base10/gates.ts, app/data/gates.ts, tests/presets/base10-adapter.test.ts</files>
  <read_first>app/data/gates.ts, app/data/types.ts, app/presets/base10/lore.ts, app/presets/base10/numogram.ts, app/presets/base10/currents.ts (adapter pattern), tests/presets/base10-adapter.test.ts, engine/index.ts</read_first>
  <behavior>
    - adapter GATE_LIST deep-equals LEGACY_GATE_LIST (hand data, still present in this task)
    - Object.keys of every entry is ['name', 'from', 'to', 'cum', 'desc', 'detail']
    - GATE_LIST.map(g => ({ name, from, to, cum })) equals the frozen oracle gates in order
    - named regressions: the entry named 'Gt-15' has from 5, to 6, cum 15; the entry named 'Gt-03' has from 2, to 3, cum 3; the list has 10 entries and starts with 'Gt-00'
    - every name equals formatGateName(BASE10.gate(k).cumulation, 10)
    - the GATE_LIST exported by app/data/gates is the adapter's array (toBe)
  </behavior>
  <action>
1. `app/presets/base10/gates.ts`: `export const GATE_LIST: GateData[]` built once: for k = 0..BASE10.zoneCount - 1, `const g = BASE10.gate(k)`, lore = `GATE_LORE[k]` (throw `Error('base-10 lore missing for gate ' + k)` if absent), entry `{ name: formatGateName(g.cumulation, 10), from: g.from, to: g.to, cum: g.cumulation, desc: lore.desc, detail: lore.detail }`. Doc comment: all n gates are emitted including Gt-00 (policy locked in Phase 2); names are in-base numerals (D-04).
2. Seam flip in `app/data/gates.ts`: rename the hand array to `export const LEGACY_GATE_LIST` (kept only until Task 2) and add `export { GATE_LIST } from '../presets/base10/gates'`.
3. Extend `tests/presets/base10-adapter.test.ts` FIRST with `describe('gates')` from <behavior> (named regressions `Gt-15 is 5 -> 6` and `Gt-03 is 2 -> 3`; LEGACY comparison in its own `it`).
4. `npx cross-env CCRUG_TZ=UTC vitest run --project oracle`, `npm run typecheck`, `MSYS_NO_PATHCONV=1 npm run test:swap`; commit `refactor(02-10): swap gates to the engine-derived base-10 adapter with own-base names (D-02, D-04)`; then `MSYS_NO_PATHCONV=1 npm run verify` on the committed tree must be green (fix defects in this plan's code only; never touch goldens, baselines or fixtures).
  </action>
  <verify>
    <automated>npx cross-env CCRUG_TZ=UTC vitest run --project oracle && MSYS_NO_PATHCONV=1 npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `app/presets/base10/gates.ts` contains `formatGateName(` and `BASE10.gate(`; `app/data/gates.ts` contains `export { GATE_LIST } from '../presets/base10/gates'` and `LEGACY_GATE_LIST`
    - `tests/presets/base10-adapter.test.ts` contains `Gt-15 is 5 -> 6`, `Gt-03 is 2 -> 3` and `describe('gates'`
    - `MSYS_NO_PATHCONV=1 npm run verify` exits 0 on the committed swap
    - `git diff --quiet f7d6689 -- app/NumogramClient.tsx app/components app/hooks app/lib e2e/__golden__ engine/test/fixtures/base10.golden.json` exits 0; `node scripts/golden-manifest.mjs verify e2e/__behaviour__/MANIFEST.json` exits 0 and `git status --porcelain -- e2e/__behaviour__` prints nothing (the behaviour baseline is created by 02-03, so it has no f7d6689 version to diff against)
  </acceptance_criteria>
  <done>The viewer's gates and their names come from the engine joined with lore, with the full gate green while the old data still exists.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the hand-authored gate data (D-08)</name>
  <files>app/data/gates.ts, tests/presets/base10-adapter.test.ts</files>
  <read_first>app/data/gates.ts, tests/presets/base10-adapter.test.ts</read_first>
  <action>
Only after Task 1's verify was green: make `app/data/gates.ts` exactly the seam comment (`// Base-10 data seam (MIG-01): gates derived by the engine and joined with lore in app/presets/base10/gates.ts.`) plus `export { GATE_LIST } from '../presets/base10/gates'`. Remove the LEGACY comparison and import from the adapter test (keep oracle, key-order, name-formatter, regression and identity checks). Run `npx cross-env CCRUG_TZ=UTC vitest run --project oracle`, `npm run typecheck`, `MSYS_NO_PATHCONV=1 npm run test:swap`, `node scripts/check-repo.mjs`. Commit `refactor(02-10): delete hand-authored gate data (D-08)`.
  </action>
  <verify>
    <automated>MSYS_NO_PATHCONV=1 npm run test:swap && npm run typecheck && node scripts/check-repo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "cum: [0-9]|'Gt-|LEGACY" app/data/gates.ts` prints 0 and the file has at most 3 non-empty lines
    - `grep -c "LEGACY_GATE_LIST" tests/presets/base10-adapter.test.ts` prints 0
    - `npm run test:swap`, `npm run typecheck` and `node scripts/check-repo.mjs` pass
  </acceptance_criteria>
  <done>No hand-authored gate structure remains; the gate is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine gate arithmetic + formatter -> viewer gate names and routing | NumogramClient keys gate render data on the name; a formatter slip would re-route or drop a gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-39 | Tampering | gate name formatting (padding, digits) | mitigate | Deep equality with the hand names before deletion; frozen oracle names; DOM goldens (layer-gates states) |
| T-02-40 | Tampering | the known diagram errors (Gt-15 drawn as 16, Gt-03 as 8) | mitigate | Named regressions in the adapter test and in the engine oracle test |
| T-02-41 | Tampering | goldens regenerated to pass | mitigate | updateSnapshots none, manifests, --clean-tree in verify, acceptance diff vs f7d6689 |
</threat_model>

<verification>
- `npm run verify` green after the swap commit; `npm run test:swap` green after the deletion
</verification>

<success_criteria>
- Gates and their own-base names are engine-derived and lore-joined with byte-identical goldens and an unchanged behaviour baseline (MIG-01, D-02 step 3, D-04/D-05 exercised)
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-core-and-base-10-migration/02-10-SUMMARY.md`
</output>
