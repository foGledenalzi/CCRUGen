---
phase: 02-engine-core-and-base-10-migration
plan: 08
type: execute
wave: 5
depends_on: ["02-06", "02-07"]
files_modified:
  - app/presets/base10/numogram.ts
  - app/presets/base10/syzygies.ts
  - app/data/syzygies.ts
  - tests/presets/base10-adapter.test.ts
  - perf/page-weight.baseline.json
autonomous: true
requirements: [MIG-01]

must_haves:
  truths:
    - "Before the first swap, the complete gate (npm run verify) is green on the committed tree with the engine core, the reference, the behaviour baseline and the lore module in place"
    - "The viewer's syzygies come from createNumogram(10) joined with SYZYGY_LORE by pair id through the base-10 adapter; app/data/syzygies.ts only re-exports the adapter (consumers unchanged, D-01)"
    - "After the swap the full gate is green (60 DOM goldens byte-identical, behaviour baseline, numeric oracle, static-export specs, npm run verify) and only then is the hand-authored syzygy data deleted (D-02, D-08)"
  artifacts:
    - path: "app/presets/base10/numogram.ts"
      provides: "BASE10 = createNumogram(10) with the base-10 preset invariants (one Torque cycle, a Warp)"
      contains: "createNumogram(10)"
    - path: "app/presets/base10/syzygies.ts"
      provides: "SYZYGIES: SyzygyData[] derived from the engine + lore"
      exports: ["SYZYGIES"]
    - path: "tests/presets/base10-adapter.test.ts"
      provides: "Adapter checks against the frozen oracle and (until deletion) the hand data"
      contains: "SYZYGIES"
  key_links:
    - from: "app/data/syzygies.ts"
      to: "app/presets/base10/syzygies.ts"
      via: "export { SYZYGIES } from '../presets/base10/syzygies'"
      pattern: "presets/base10/syzygies"
    - from: "app/presets/base10/numogram.ts"
      to: "engine/index.ts"
      via: "relative import ../../../engine/index"
      pattern: "engine/index"
---

<objective>
First data-source swap (D-02 order: syzygies, currents, gates, regions/zones, demons): create the base-10 adapter's engine instance and derive `SYZYGIES` from the engine joined with lore by id, flip the `app/data/syzygies.ts` seam to it, prove the full gate green, then delete the hand-authored syzygy structure.

Purpose: MIG-01, strangler step 1. The seam approach keeps NumogramClient.tsx, app/components/** and app/components/projection/** byte-identical (D-01, D-03): `app/data/syzygies.ts` stays as a thin re-export module so no consumer import changes.
Output: adapter instance + syzygies adapter, rewired seam, adapter test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-engine-core-and-base-10-migration/02-CONTEXT.md
@CLAUDE.md
@app/data/types.ts
@app/data/syzygies.ts
@app/presets/base10/lore.ts
@engine/index.ts
@tests/oracle/deriveBase10.ts
@scripts/page-weight.mjs
@package.json

<interfaces>
- `SyzygyData { a: number; b: number; demon: string; desc: string }` (app/data/types.ts). Today's order: (4,5), (3,6), (2,7), (1,8), (0,9): pair ids descending, `a` = low zone, `b` = high zone. Key order of each object: a, b, demon, desc.
- `SYZYGY_LORE: Record<number, { demon, desc }>` keyed by pair id; `DEMON_NAMES: Record<number, string>` keyed by mesh (app/presets/base10/lore.ts, from 02-07).
- Engine: `createNumogram(10)`: `pairCount` 5, `pair(id)` -> `{ id, lo, hi, odd, even }`, `torqueCount`, `warp`, `demons.meshOf(a, b)`.
- Frozen oracle `engine/test/fixtures/base10.golden.json`: `pairs` [[4,5],[3,6],[2,7],[1,8],[0,9]].
- Consumers of `SYZYGIES` (NOT edited): app/NumogramClient.tsx, app/components/projection/Projection.tsx, app/components/panels/SyzygiesPanel.tsx, app/components/info/InfoDisplay.tsx, tests/oracle/deriveBase10.ts (the oracle-equality test then checks the engine-derived data against the frozen JSON automatically).
- Gates: `npm run test:swap` = build + `playwright test e2e/golden.spec.ts e2e/behaviour.spec.ts` + `npm run test`; full gate `MSYS_NO_PATHCONV=1 npm run verify` (its last step `check-repo --clean-tree` needs a committed tree, so run it after the commit). Page weight: `node scripts/page-weight.mjs check|update --reason "<why>"` (update only after a root `npm run build`).
</interfaces>

Rules: never edit NumogramClient.tsx, app/components/** (including projection/**), app/hooks/**, or any hard-coded 10-zone logic (D-01, D-03, Phase 4 owns MIG-02). Never regenerate goldens, baselines or fixtures; never `-u`. Import the engine by relative path (`../../../engine/index`), not `@/` (Vitest does not read tsconfig paths).

Commits: end every commit message with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Never `git push`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-flight gate, base-10 engine instance, syzygies adapter and seam flip (swap commit)</name>
  <files>app/presets/base10/numogram.ts, app/presets/base10/syzygies.ts, app/data/syzygies.ts, tests/presets/base10-adapter.test.ts, perf/page-weight.baseline.json</files>
  <read_first>app/data/syzygies.ts, app/data/types.ts, app/presets/base10/lore.ts, engine/index.ts, engine/core/types.ts, tests/oracle/deriveBase10.ts, scripts/page-weight.mjs, perf/page-weight.baseline.json</read_first>
  <behavior>
    - adapter SYZYGIES deep-equals LEGACY_SYZYGIES (the hand data, still present in this task)
    - Object.keys of every adapter entry is ['a', 'b', 'demon', 'desc']
    - SYZYGIES.map(s => [s.a, s.b]) equals the frozen oracle pairs [[4,5],[3,6],[2,7],[1,8],[0,9]]
    - every entry's demon equals DEMON_NAMES[BASE10.demons.meshOf(s.b, s.a)]
    - the seam re-exports the adapter array itself: the SYZYGIES imported from app/data/syzygies is the same object (toBe) as the adapter's
    - BASE10.base === 10, BASE10.torqueCount === 1, BASE10.warp !== null
  </behavior>
  <action>
0. Pre-flight (first swap, D-02): on the committed tree run `MSYS_NO_PATHCONV=1 npm run verify`. It must be green before anything is changed. If it is red, fix the cause only when it lies in a file created by plans 02-01..02-07 and the fix touches no frozen artifact (goldens, baselines, fixtures, manifests); otherwise STOP and report. Record the verify runtime in the SUMMARY.
1. `app/presets/base10/numogram.ts`: header comment "Base-10 engine instance for the preset adapter (MIG-01, D-01). Original CCRUG code (MIT, NOTICE section 1)."; `import { createNumogram } from '../../../engine/index'`; `export const BASE10 = createNumogram(10)`; then `if (BASE10.torqueCount !== 1 || BASE10.warp === null) throw new Error('base-10 preset: expected exactly one Torque cycle and a Warp')`.
2. `app/presets/base10/syzygies.ts`: `export const SYZYGIES: SyzygyData[]` built once at module load: pair ids from `BASE10.pairCount - 1` down to 0 (doc comment: "upstream panel order: pair ids descending"), each `{ a: lo, b: hi, demon: lore.demon, desc: lore.desc }` from `BASE10.pair(id)` and `SYZYGY_LORE[id]` (throw `Error('base-10 lore missing for syzygy ' + id)` if absent). Literal key order a, b, demon, desc.
3. Seam flip in `app/data/syzygies.ts`: rename the hand array to `export const LEGACY_SYZYGIES: SyzygyData[]` (kept only until Task 2, comment says so) and add `export { SYZYGIES } from '../presets/base10/syzygies'`.
4. `tests/presets/base10-adapter.test.ts` (oracle project) FIRST from <behavior>: a `describe('syzygies')` block; the LEGACY comparison sits in its own `it('equals the hand-authored data until it is deleted (plan 02-08 Task 2)')`.
5. Quick checks: `npx cross-env CCRUG_TZ=UTC vitest run --project oracle` (adapter + oracle-equality + lore tests), `npm run typecheck`. Then `MSYS_NO_PATHCONV=1 npm run test:swap` (build, 60 goldens, behaviour baseline, vitest). All green, then commit `refactor(02-08): swap syzygies to the engine-derived base-10 adapter (D-02)`.
6. Full gate on the committed tree: `MSYS_NO_PATHCONV=1 npm run verify`. If ONLY `check:weight` fails and the growth is the engine code now bundled into /numogram/ (this is the first swap that imports the engine), run `npm run build` then `node scripts/page-weight.mjs update --reason "Phase 2 swap 1: the pure engine (engine/core) is now part of the /numogram/ bundle; +<N> bytes raw / +<M> gzip, no other growth"`, commit `perf/page-weight.baseline.json` as `perf(02-08): page-weight baseline includes the engine bundle`, and rerun verify. Any other failure is a defect in this plan's code: fix it (never by touching goldens, baselines or fixtures).
  </action>
  <verify>
    <automated>npx cross-env CCRUG_TZ=UTC vitest run --project oracle && MSYS_NO_PATHCONV=1 npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `app/data/syzygies.ts` contains `export { SYZYGIES } from '../presets/base10/syzygies'` and `LEGACY_SYZYGIES`
    - `app/presets/base10/numogram.ts` contains `createNumogram(10)` and `../../../engine/index`; `app/presets/base10/syzygies.ts` contains `SYZYGY_LORE` and `BASE10.pair(`
    - `tests/presets/base10-adapter.test.ts` contains `LEGACY_SYZYGIES`, `toBe(` and `[[4, 5], [3, 6], [2, 7], [1, 8], [0, 9]]` (or reads golden.pairs)
    - `MSYS_NO_PATHCONV=1 npm run verify` exits 0 on the committed swap (pre-flight and post-swap runtimes in the SUMMARY)
    - `git diff --quiet f7d6689 -- app/NumogramClient.tsx app/components app/hooks app/lib e2e/__golden__ engine/test/fixtures/base10.golden.json` exits 0; `node scripts/golden-manifest.mjs verify e2e/__behaviour__/MANIFEST.json` exits 0 and `git status --porcelain -- e2e/__behaviour__` prints nothing (the behaviour baseline is created by 02-03, so it has no f7d6689 version to diff against)
  </acceptance_criteria>
  <done>The viewer's syzygies come from the engine joined with lore, and the complete gate is green with the old data still present.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the hand-authored syzygy data (D-08)</name>
  <files>app/data/syzygies.ts, tests/presets/base10-adapter.test.ts</files>
  <read_first>app/data/syzygies.ts, tests/presets/base10-adapter.test.ts</read_first>
  <action>
Only after Task 1's `npm run verify` was green on the committed swap: make `app/data/syzygies.ts` exactly the seam comment (`// Base-10 data seam (MIG-01): syzygies derived by the engine and joined with lore in app/presets/base10/syzygies.ts.`) plus `export { SYZYGIES } from '../presets/base10/syzygies'` (no LEGACY_SYZYGIES, no SYZYGY_LORE import, no literal zones). Remove the LEGACY comparison and its import from `tests/presets/base10-adapter.test.ts` (keep the oracle, key-order, demon-name and identity checks). No tests/legacy copy (D-08): the frozen oracle, the DOM goldens and the behaviour baseline guard the values. Run `npx cross-env CCRUG_TZ=UTC vitest run --project oracle`, `npm run typecheck`, `MSYS_NO_PATHCONV=1 npm run test:swap`, `node scripts/check-repo.mjs`. Commit `refactor(02-08): delete hand-authored syzygy data (D-08)`.
  </action>
  <verify>
    <automated>MSYS_NO_PATHCONV=1 npm run test:swap && npm run typecheck && node scripts/check-repo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "a: [0-9]|b: [0-9]|LEGACY" app/data/syzygies.ts` prints 0 and the file has at most 3 non-empty lines
    - `grep -c "LEGACY_SYZYGIES" tests/presets/base10-adapter.test.ts` prints 0
    - `npm run test:swap`, `npm run typecheck` and `node scripts/check-repo.mjs` pass
  </acceptance_criteria>
  <done>No hand-authored syzygy structure remains; the gate is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine + lore (by id) -> rendered viewer | A mis-join or order change becomes visible text or SVG |
| frozen oracles -> merge decision | The swap is accepted only if every frozen oracle passes unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-32 | Tampering | goldens/baselines regenerated to make the swap pass | mitigate | updateSnapshots none, capture only for missing files, manifests, check-repo --clean-tree inside verify; acceptance diff against f7d6689 |
| T-02-33 | Tampering | lore joined to the wrong pair | mitigate | Adapter test vs hand data (while present), frozen pairs, syzygy/demon name consistency, behaviour baseline lore sweep |
| T-02-34 | Denial of Service | bundle growth from the engine | mitigate | page-weight budget in verify; baseline raised only for the engine bundle with a written reason |
| T-02-35 | Tampering | consumer files edited during migration | mitigate | Seam re-export instead of import edits; acceptance diff on NumogramClient.tsx, components, hooks, lib |
</threat_model>

<verification>
- `npm run verify` green after the swap commit; `npm run test:swap` green after the deletion
- No consumer or frozen file changed since f7d6689
</verification>

<success_criteria>
- Syzygies are engine-derived and lore-joined with byte-identical goldens and an unchanged behaviour baseline (MIG-01, D-02 step 1)
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-core-and-base-10-migration/02-08-SUMMARY.md`
</output>
