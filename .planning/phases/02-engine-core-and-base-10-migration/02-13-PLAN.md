---
phase: 02-engine-core-and-base-10-migration
plan: 13
type: execute
wave: 10
depends_on: ["02-12"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [ENG-01, ENG-02, ENG-03, ENG-04, ENG-05, MIG-01]

must_haves:
  truths:
    - "The complete gate (MSYS_NO_PATHCONV=1 npm run verify) is green on the final committed tree, and its evidence for ROADMAP Phase 2 success criteria 1-5 is recorded"
    - "Nothing frozen before this phase changed (base10.golden.json, its manifest, e2e/__golden__), every manifest verifies, and the only app/ files changed since f7d6689 are the five app/data seams, app/lib/constants.ts and app/presets/base10/*"
    - "ROADMAP.md shows the 13 Phase 2 plans checked and 13/13 in the progress table; STATE.md points at /gsd-verify-work 2 with the Phase 2 decisions; no requirement is marked complete by this plan"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 plan list checked, progress row 13/13"
      contains: "- [x] 02-13-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "Position after Phase 2 execution and Phase 2 decisions"
      contains: "gsd-verify-work 2"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/phases/02-engine-core-and-base-10-migration/*-SUMMARY.md"
      via: "decisions carried from the plan summaries"
      pattern: "Phase 2"
---

<objective>
Close Phase 2's execution: run the complete gate on the final tree, collect the evidence for the five ROADMAP success criteria and the phase-wide invariants (frozen oracles untouched, only the data seams changed under app/, lore in one file, no hand structure left), then update ROADMAP.md (plan checkboxes and progress row, by hand: the SDK's roadmap.update-plan-progress cannot match this ROADMAP) and STATE.md.

Purpose: a verifiable hand-off to `/gsd-verify-work 2`. Requirement status is set by the orchestrator at phase level, not here.
Output: SUMMARY with the evidence; ROADMAP.md and STATE.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-engine-core-and-base-10-migration/02-CONTEXT.md
@.planning/phases/02-engine-core-and-base-10-migration/02-VALIDATION.md
@CLAUDE.md

Evidence map (ROADMAP Phase 2 success criteria -> tests created in this phase):
1. Base-10 engine output equals the frozen oracle (10 zones, 5 pairs, currents, gates incl. Gt-15 = 5->6 and Gt-03 = 2->3, regions, 45 demons 12+3 / 12+12 / 4+2): engine/test/base10.engine.test.ts, tests/oracle/base10.oracle.test.ts (now fed by the engine through the seams), tests/presets/base10-adapter.test.ts.
2. Guide facts (base 12, 16 [4,2], 28 [9,3], 80 [39], 82 [27,9,3], Warp iff n = 3o+1 with o odd), sweep of every even n <= 2000, invalid bases rejected: engine/test/notable-bases.test.ts, engine/test/structure.sweep.test.ts, engine/test/base.test.ts, engine/test/numogram.test.ts, tests/bruteforce/*.
3. Base 28 = 378 demons / 108 cross-Torque, O(1) mesh <-> net-span, counts sum to C(n,2), Numodemons n/2 - 1: engine/test/demons.test.ts, engine/test/demons.sweep.test.ts, engine/test/demons.unrank.test.ts.
4. Base 2^26 without O(n^2), above refused: engine/test/ceiling.test.ts (or the test:heavy fallback recorded in 02-04's SUMMARY).
5. The viewer's syzygies, currents, gates, demons and regions come from the engine joined with lore by id, DOM goldens byte-identical, layouts/hover/undo/share unchanged: e2e/golden.spec.ts (60), e2e/behaviour.spec.ts (D-15 baseline), tests/presets/base10-lore.test.ts, tests/presets/base10-adapter.test.ts.

Rules: do not edit source, tests, goldens, baselines or fixtures in this plan; if the gate is red, STOP and report which plan's area failed (do not patch around it here). Do not mark requirements complete in REQUIREMENTS.md and do not tick the Phase 2 line in the ROADMAP phase list (the orchestrator does both at phase level). Never `git push` (pushing is user-approved and outside all plans). Commits end with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final phase gate and evidence</name>
  <files>.planning/phases/02-engine-core-and-base-10-migration/02-13-SUMMARY.md</files>
  <read_first>.planning/phases/02-engine-core-and-base-10-migration/02-VALIDATION.md, the SUMMARY files 02-01..02-12 in the phase directory, package.json (verify chain), scripts/check-repo.mjs</read_first>
  <action>
Run, on the committed tree, and paste the relevant output lines into the SUMMARY:
1. `MSYS_NO_PATHCONV=1 npm run verify` (record wall time; expect repo guard, typecheck, vitest UTC + America/New_York, sub-path e2e, build, page weight, e2e with 60 goldens and the 5 behaviour tests, clean-tree/static-out all green).
2. Frozen and manifests: `git diff --quiet f7d6689 -- e2e/__golden__ engine/test/fixtures/base10.golden.json engine/test/fixtures/MANIFEST.json` exits 0; `node scripts/golden-manifest.mjs verify e2e/__golden__/MANIFEST.json engine/test/fixtures/MANIFEST.json engine/test/fixtures/derived/MANIFEST.json e2e/__behaviour__/MANIFEST.json` exits 0; `git status --porcelain -- e2e/__golden__ e2e/__behaviour__ engine/test/fixtures` prints nothing.
3. Scope of app/ changes: `git diff --name-only f7d6689 -- app` prints only `app/data/zones.ts`, `app/data/syzygies.ts`, `app/data/currents.ts`, `app/data/gates.ts`, `app/data/demons.ts`, `app/lib/constants.ts` and files under `app/presets/base10/` (D-01, D-03).
4. Lore and hand structure: `grep -rl "CCRU-derived lore" app` prints exactly `app/presets/base10/lore.ts`; `grep -cE "(from|to|cum|a|b): [0-9]|new Set\(\[|push\(|'Gt-" app/data/zones.ts app/data/syzygies.ts app/data/currents.ts app/data/gates.ts app/data/demons.ts` prints 0 for every file; `grep -cE "\[\[1, 8\]|'Surge'" app/lib/constants.ts` prints 0.
5. Engine purity and independence: `grep -rnE "from '(\.\./)+(app|tests|scripts)" engine/core engine/index.ts` prints nothing; `grep -rnE "engine/(core|index)" tests/bruteforce/numogramReference.ts` prints nothing; `git ls-files reference | wc -l` prints 0 (the gitignored root local-sources folder); `git ls-files tests/bruteforce` lists the three brute-force files.
6. Evidence table in the SUMMARY: for each ROADMAP Phase 2 success criterion 1-5 and each requirement ENG-01..ENG-05, MIG-01, the test files and the passing command (use the evidence map in <context>), plus the D-11 outcome (2^26 inside verify or test:heavy) and the measured ceiling line.
Commit the SUMMARY only if the executor workflow does so for verification-only tasks; otherwise it is committed with Task 2.
  </action>
  <verify>
    <automated>MSYS_NO_PATHCONV=1 npm run verify && git diff --quiet f7d6689 -- e2e/__golden__ engine/test/fixtures/base10.golden.json engine/test/fixtures/MANIFEST.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify` exits 0 on the final committed tree (output lines and wall time in the SUMMARY)
    - All four manifests verify; the frozen pre-phase files are byte-identical to f7d6689
    - `git diff --name-only f7d6689 -- app` lists only the allowed files; `grep -rl "CCRU-derived lore" app` prints only app/presets/base10/lore.ts
    - The SUMMARY contains an evidence table with rows for success criteria 1, 2, 3, 4, 5 and for ENG-01, ENG-02, ENG-03, ENG-04, ENG-05, MIG-01
  </acceptance_criteria>
  <done>The phase is proven green end to end with recorded evidence for every criterion and requirement.</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP and STATE bookkeeping</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/ROADMAP.md, .planning/STATE.md, the SUMMARY files 02-01..02-13</read_first>
  <action>
1. `.planning/ROADMAP.md` (hand edit): in the Phase 2 "Plans:" list change every `- [ ] 02-NN-PLAN.md` (NN = 01..13) to `- [x] 02-NN-PLAN.md`, keeping each line's text; in the Progress table change the Phase 2 row to `| 2. Engine Core and Base-10 Migration | 13/13 | Executed, verification pending | - |`. Do not tick `- [ ] **Phase 2: ...**` in the phase list and do not touch other phases.
2. `.planning/STATE.md`: frontmatter `status` and `stopped_at` describe "Phase 2 executed (13/13 plans); next /gsd-verify-work 2"; update `last_updated`/`last_activity` and the progress counters consistently (use `gsd-sdk query` state helpers if available, else edit by hand); "Current focus" = Phase 2 - Engine Core and Base-10 Migration; "Current Position" = Phase 2, Plan 13 of 13, Status "Executed, awaiting /gsd-verify-work 2"; add per-plan rows to the performance table from the SUMMARY durations; append "Recent decisions" lines for Phase 2 taken from the SUMMARYs, at least: the numeral scheme (0-9a-z to base 36, dot-separated decimal digit groups beyond; Gt-NN via formatGateName), canonical cycle order (Torque cycles by length desc then smallest zone, then Plex, then Warp), the seam approach (five thin pass-through files app/data/zones.ts, syzygies.ts, currents.ts, gates.ts and demons.ts remain until Phase 4 so consumers stay untouched, and app/lib/constants.ts re-exports the three Torque-circuit constants as a deliberate seam extension), the D-11 outcome with the measured numbers, the behaviour baseline location (e2e/__behaviour__, manifest-protected), any page-weight baseline raise and its reason; set "Session Continuity" to the resume point `/gsd-verify-work 2`. Do not edit REQUIREMENTS.md.
3. Commit `docs(02-13): phase 2 roadmap and state bookkeeping` with the SUMMARY if not yet committed. Do not push.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/ROADMAP.md','utf8');const n=(t.match(/- \[x\] 02-\d\d-PLAN\.md/g)||[]).length;process.exit(n===13&&t.includes('| 2. Engine Core and Base-10 Migration | 13/13 |')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains 13 lines matching `- [x] 02-NN-PLAN.md` and the row `| 2. Engine Core and Base-10 Migration | 13/13 |`, and still contains `- [ ] **Phase 2: Engine Core and Base-10 Migration**`
    - `.planning/STATE.md` contains `gsd-verify-work 2` and at least four Phase 2 decision lines
    - `git log --format= --name-only --grep='(02-13)' | grep -c 'REQUIREMENTS.md'` prints 0 (this plan's own commits do not edit REQUIREMENTS.md; the orchestrator marks requirements at phase level)
    - No `git push` was run
  </acceptance_criteria>
  <done>ROADMAP and STATE reflect the executed phase and point at verification; requirement status is left to the orchestrator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execution results -> planning docs | Docs must only claim what the gate proved |
| local repo -> remote | Pushing is user-approved and outside all plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-48 | Repudiation | completion claims without evidence | mitigate | Verify output, manifest checks and scope greps pasted into the SUMMARY evidence table |
| T-02-49 | Tampering | frozen oracles drifted during the phase | mitigate | `git diff --quiet f7d6689` on the pre-phase frozen files and verification of all four manifests |
| T-02-50 | Elevation of Privilege | unapproved push | mitigate | No push step anywhere; acceptance states no push was run |
</threat_model>

<verification>
- `npm run verify` green; frozen files byte-identical to f7d6689; ROADMAP/STATE updated
</verification>

<success_criteria>
- Phase 2 is executed, proven green with per-criterion evidence, and handed to /gsd-verify-work 2
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-core-and-base-10-migration/02-13-SUMMARY.md`
</output>
