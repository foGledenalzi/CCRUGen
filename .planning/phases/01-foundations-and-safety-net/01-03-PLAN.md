---
phase: 01-foundations-and-safety-net
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/oracle/deriveBase10.ts
  - scripts/capture-base10-oracle.ts
  - engine/test/fixtures/base10.golden.json
  - engine/test/fixtures/MANIFEST.json
  - tests/oracle/base10.oracle.test.ts
autonomous: true
requirements: [FND-02]

must_haves:
  truths:
    - "A numeric base10.golden.json captured from the untouched app/data/* exists and is sha256-frozen with a written reason"
    - "The oracle test fails if app/data/* ever derives something different from the frozen JSON"
    - "Definition-derived facts hold against the frozen JSON: gate k goes to the digital root of T(k) (Gt-15 = 5->6, Gt-03 = 2->3), 5 syzygy pairs summing to 9, 45 demons split 12+3 chrono, 12 Plex + 12 Warp amphi, 4+2 xeno, currents land on |a-b|, regions are the current cycles"
    - "The oracle is identical under CCRUG_TZ=UTC and CCRUG_TZ=America/New_York"
    - "The capture script refuses to overwrite an existing oracle"
  artifacts:
    - path: "engine/test/fixtures/base10.golden.json"
      provides: "Frozen numeric base-10 oracle (pairs, currents, gates, regions, TC_*, demons, layouts, planetary constants)"
      contains: "\"demonCount\": 45"
    - path: "engine/test/fixtures/MANIFEST.json"
      provides: "sha256 freeze of the numeric oracle"
      contains: "base10.golden.json"
    - path: "tests/oracle/deriveBase10.ts"
      provides: "deriveBase10Oracle() and the Base10Oracle type"
      exports: ["deriveBase10Oracle", "Base10Oracle"]
    - path: "tests/oracle/base10.oracle.test.ts"
      provides: "Oracle equality + definitional regression tests"
      min_lines: 80
  key_links:
    - from: "tests/oracle/deriveBase10.ts"
      to: "app/data/*.ts, app/lib/planetary.ts, app/lib/constants.ts"
      via: "relative imports (app-side, never from engine/)"
      pattern: "\\.\\./\\.\\./app/data/"
    - from: "tests/oracle/base10.oracle.test.ts"
      to: "engine/test/fixtures/base10.golden.json"
      via: "readFileSync + JSON.parse"
      pattern: "base10.golden.json"
---

<objective>
Capture the numeric half of the base-10 oracle (D-13) from the untouched `app/data/*`: a frozen `engine/test/fixtures/base10.golden.json`, a sha256 manifest entry, and a Vitest oracle test with named regressions for Gt-15 (5->6) and Gt-03 (2->3). Planetary is checked numerically only (its constants and default-angle positions).

Purpose: FND-02. Phase 2's engine must reproduce this JSON exactly; the base-10 strangler migration (MIG-01) is guarded by it. The oracle records what the viewer shows today; nothing in it is "fixed".
Output: derivation module, one-shot capture script, frozen JSON + manifest, oracle test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-and-safety-net/01-CONTEXT.md
@.planning/phases/01-foundations-and-safety-net/01-RESEARCH.md
@CLAUDE.md
@app/data/types.ts
@app/data/syzygies.ts
@app/data/currents.ts
@app/data/gates.ts
@app/data/demons.ts
@app/data/positions.ts
@app/lib/planetary.ts
@app/lib/constants.ts

<interfaces>
Existing exports this plan reads (do NOT modify any of these files):
- `app/data/syzygies.ts`: `SYZYGIES: SyzygyData[]` with `{ a, b, demon, desc }`; data order (4,5), (3,6), (2,7), (1,8), (0,9)
- `app/data/currents.ts`: `CURRENTS: CurrentData[]` with `{ name, from, to, label, desc }`: Surge 8->7, Hold 2->5, Sink 4->1, Warp 6->3, Plex 9->9
- `app/data/gates.ts`: `GATE_LIST: GateData[]` with `{ name, from, to, cum, desc, detail }`: Gt-00 0->0 (0), Gt-01 1->1 (1), Gt-03 2->3 (3), Gt-06 3->6 (6), Gt-10 4->1 (10), Gt-15 5->6 (15), Gt-21 6->3 (21), Gt-28 7->1 (28), Gt-36 8->9 (36), Gt-45 9->9 (45)
- `app/data/demons.ts`: `DEMON_NAMES: Record<string,string>` keyed `"i:j"`; `TC = new Set([1, 2, 4, 5, 7, 8])`; `ALL_DEMONS: Demon[]` built for i in 1..9, j < i, `kind` = `'syzygy'` if i+j===9, else `'chrono'` if both in TC, `'xeno'` if neither, else `'amphi'`
- `app/data/zones.ts`: `ZONE_REGION: Record<number, Region>`: 0 plex, 1 torque, 2 torque, 3 warp, 4 torque, 5 torque, 6 warp, 7 torque, 8 torque, 9 plex
- `app/data/positions.ts`: `P_ORIGINAL`, `P_LABYRINTH`, `P_LADDER: Record<number, Pos>`, `PLANETARY_CX = 400`, `PLANETARY_CY = 400`, `PLANETARY_RADIUS`, `PLANETARY_DEFAULT_ANGLE`, `PLANETARY_SIZE: Record<number, number>`, `CENTER: Record<Layout, Pos>`
- `app/lib/planetary.ts`: `computePlanetaryPositions(angles: Record<number, number>): Record<number, Pos>` (x = cx + cos((angle-90)*PI/180)*r, y = cy + sin(...)*r)
- `app/lib/constants.ts`: `TC_EDGES: [number, number][]`, `TC_CURRENTS = new Set(['Surge','Hold','Sink'])`, `TC_SYZYGIES: [number, number][]`
- `app/data/types.ts`: `Region = 'torque' | 'warp' | 'plex'`, `Pos = { x: number; y: number }`
- From plan 01-01: `scripts/golden-manifest.mjs freeze <manifest> --set <YYYY-MM-DD-slug> --reason "<why>" <file>`; Vitest project `oracle` includes `tests/**/*.test.ts`
</interfaces>

Rules (CLAUDE.md, D-13, D-15): the oracle is derived from `app/data/*` and checked against DEFINITIONS, never against `reference/` or the guide's prose (its demon subtype counts are wrong: the correct split is 45 = 12+3 chrono, 12+12 amphi, 4+2 xeno). Digital root is always `T === 0 ? 0 : ((T - 1) % (n - 1)) + 1`, never a decimal digit sum. Never read `reference/` from code or tests. The frozen JSON is never regenerated (`vitest -u` does not apply; the capture script refuses to overwrite).

Wave-2 note: plans 01-02 and 01-04 run in the same wave. Do not edit their files (`.eslintrc.json`, `engine/test/guard.test.ts`, `e2e/**`, `playwright.config.ts`, `tests/e2e-normalizer/**`). `app/**` must stay untouched by every Wave-2 plan.

Commits: end every commit message with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive and capture the frozen numeric oracle</name>
  <files>tests/oracle/deriveBase10.ts, scripts/capture-base10-oracle.ts, engine/test/fixtures/base10.golden.json, engine/test/fixtures/MANIFEST.json</files>
  <read_first>app/data/types.ts, app/data/syzygies.ts, app/data/currents.ts, app/data/gates.ts, app/data/demons.ts, app/data/zones.ts, app/data/positions.ts, app/lib/planetary.ts, app/lib/constants.ts, scripts/golden-manifest.mjs</read_first>
  <action>
0. Pre-flight (the viewer data must be untouched): `git diff --quiet 7c38ad9 HEAD -- app && git diff --quiet -- app` must exit 0. If not, STOP and report.

1. Create `tests/oracle/deriveBase10.ts` (app-side helper; imports app code by relative path so `engine/` never imports `app/`). Export `interface Base10Oracle` and `function deriveBase10Oracle(): Base10Oracle` producing, in this key order:
   - `schema: 1`, `base: 10`, `source: 'app/data/* and app/lib/{planetary,constants}.ts at upstream commit 7c38ad9 (untouched viewer)'`
   - `zoneCount`: `Object.keys(ZONE_REGION).length`
   - `pairs`: `SYZYGIES.map(s => [min(a,b), max(a,b)])` in data order (typed `[number, number][]`)
   - `currents`: `CURRENTS.map(c => ({ name: c.name, pair: <sorted syzygy pair containing c.from>, from: c.from, to: c.to }))` (D-13 "currents normalized to pair + destination"; throw if a zone has no syzygy)
   - `gates`: `GATE_LIST.map(g => ({ name: g.name, from: g.from, to: g.to, cum: g.cum }))`
   - `zoneRegion`: `{ ...ZONE_REGION }`
   - `regions`: `{ plex: number[], torque: number[], warp: number[] }` built by iterating zones 0..9 ascending (expected `plex [0,9]`, `torque [1,2,4,5,7,8]`, `warp [3,6]`)
   - `tc`: `{ zones: Array.from(TC).sort(numeric), edges: TC_EDGES copied as tuples, currents: Array.from(TC_CURRENTS).sort(), syzygies: TC_SYZYGIES copied as tuples }`
   - `demons`: `ALL_DEMONS.map(d => ({ a: d.a, b: d.b, netSpan: \`${d.a}::${d.b}\`, kind: d.kind, name: d.name }))` (data order)
   - `demonCount`: `ALL_DEMONS.length`
   - `kinds`: counts of `d.kind`, keys inserted in alphabetical order (expected `{ amphi: 24, chrono: 12, syzygy: 5, xeno: 4 }`)
   - `syzygeticBy`: for demons with kind `syzygy`, count `'chrono'` if both zones are in TC, `'xeno'` if neither (alphabetical keys; expected `{ chrono: 3, xeno: 2 }`)
   - `amphiBy`: for demons with kind `amphi`, count `ZONE_REGION[the zone NOT in TC]` (alphabetical keys; expected `{ plex: 12, warp: 12 }`)
   - `layouts`: `{ original: P_ORIGINAL, labyrinth: P_LABYRINTH, ladder: P_LADDER }`, `center: CENTER`
   - `planetary`: `{ cx: PLANETARY_CX, cy: PLANETARY_CY, radius: PLANETARY_RADIUS, defaultAngle: PLANETARY_DEFAULT_ANGLE, size: PLANETARY_SIZE, positionsAtDefaultAngle: computePlanetaryPositions(PLANETARY_DEFAULT_ANGLE) }`
   Header comment: "Derives the frozen base-10 numeric oracle from the untouched upstream data. Records what the viewer shows today; never 'fix' values here." Nothing in it may depend on the date or timezone.

2. Create `scripts/capture-base10-oracle.ts` (one-shot, run with `npx tsx`; do NOT use `import.meta` because tsx runs this non-ESM package's `.ts` as CJS; resolve paths from `process.cwd()`):
```ts
// One-time capture of the frozen base-10 numeric oracle (FND-02, D-13).
// Refuses to overwrite: the oracle is never regenerated (CLAUDE.md, D-15). Run from the repo root.
import { existsSync, mkdirSync, writeFileSync } from 'node:fs'
import { dirname, resolve } from 'node:path'
import { deriveBase10Oracle } from '../tests/oracle/deriveBase10'

const OUT = resolve('engine/test/fixtures/base10.golden.json')
if (!existsSync(resolve('app/data/zones.ts'))) {
  console.error('capture-base10-oracle: run from the repository root')
  process.exit(2)
}
if (existsSync(OUT)) {
  console.error('capture-base10-oracle: engine/test/fixtures/base10.golden.json exists; the base-10 oracle is frozen and is never regenerated')
  process.exit(1)
}
mkdirSync(dirname(OUT), { recursive: true })
const text = JSON.stringify(deriveBase10Oracle(), null, 2) + '\n'
writeFileSync(OUT, text, 'utf8')
console.log(`capture-base10-oracle: wrote ${Buffer.byteLength(text)} bytes`)
```

3. Run `npx tsx scripts/capture-base10-oracle.ts` once (expect roughly 5-12 KB; the lab prototype without names/net-spans was 5.3 KB). Run it a second time and confirm it exits 1 with `is frozen and is never regenerated` and the file is unchanged. Confirm the file has no `\r`: `node -e "process.exit(require('fs').readFileSync('engine/test/fixtures/base10.golden.json').includes(13)?1:0)"`.

4. Freeze it: `node scripts/golden-manifest.mjs freeze engine/test/fixtures/MANIFEST.json --set <YYYY-MM-DD>-base10-numeric --reason "Numeric base-10 oracle (D-13) captured with tsx from the untouched app/data/* at upstream 7c38ad9, before any Phase 1 render-path change" engine/test/fixtures/base10.golden.json` (use the execution date in UTC). Then `node scripts/golden-manifest.mjs verify engine/test/fixtures/MANIFEST.json` must exit 0.

Commit the four files.
  </action>
  <verify>
    <automated>node scripts/golden-manifest.mjs verify engine/test/fixtures/MANIFEST.json && node -e "const g=require('./engine/test/fixtures/base10.golden.json');const ok=g.base===10&&g.demonCount===45&&g.pairs.length===5&&g.gates.length===10&&g.currents.length===5&&JSON.stringify(g.kinds)==='{\"amphi\":24,\"chrono\":12,\"syzygy\":5,\"xeno\":4}';process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `engine/test/fixtures/base10.golden.json` exists, ends with `\n`, contains no `\r`, and contains `"demonCount": 45`, `"name": "Gt-15"`, `"netSpan": "9::0"`
    - The JSON `kinds` equals `{"amphi":24,"chrono":12,"syzygy":5,"xeno":4}`, `syzygeticBy` equals `{"chrono":3,"xeno":2}`, `amphiBy` equals `{"plex":12,"warp":12}`
    - `engine/test/fixtures/MANIFEST.json` contains one set whose name ends in `-base10-numeric` with a non-empty `reason` and the key `engine/test/fixtures/base10.golden.json`
    - A second `npx tsx scripts/capture-base10-oracle.ts` exits 1 without modifying the file
    - `git diff --quiet 7c38ad9 HEAD -- app` still exits 0 (no app file touched)
  </acceptance_criteria>
  <done>The numeric oracle is captured from the untouched data, sha256-frozen with a reason, and cannot be regenerated by the capture script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Oracle test with definition-derived regressions</name>
  <files>tests/oracle/base10.oracle.test.ts</files>
  <read_first>tests/oracle/deriveBase10.ts, engine/test/fixtures/base10.golden.json, vitest.config.mts, CLAUDE.md</read_first>
  <behavior>
    - deriveBase10Oracle() deep-equals the frozen JSON (planetary positions compared with 1e-9 tolerance, everything else exact)
    - For every k in 0..9 exactly one gate has from k, cum T(k) = k(k+1)/2, to = digitalRoot(T(k)) with digitalRoot(T) = T===0 ? 0 : ((T-1) % 9) + 1, and name `Gt-` + two-digit T(k)
    - Gate Gt-15 is 5 -> 6 and Gate Gt-03 is 2 -> 3 (named regressions)
    - The 5 pairs each sum to 9 and cover zones 0..9 exactly once
    - Each current's pair contains its `from` zone and its `to` equals |a - b|
    - Following current(pair) = pair containing |a-b| yields exactly three cycles: [(0,9)], [(3,6)] and one 3-cycle of (1,8),(2,7),(4,5); zoneRegion maps the cycle containing 0 to plex, the 3-cycle to torque, the rest to warp
    - demons: 45 = 10*9/2 unique unordered pairs with a > b; each kind equals the definitional classification from the TC set; chrono total 15 = 12 + 3 syzygetic, xeno total 6 = 4 + 2 syzygetic, amphi 24 = 12 plex + 12 warp
    - tc.zones equals the torque region; tc.syzygies equals the torque pairs
    - planetary zone 0 sits at (400, 400) and every positionsAtDefaultAngle entry matches cx + cos((angle-90)*PI/180)*r within 1e-9
  </behavior>
  <action>
Create `tests/oracle/base10.oracle.test.ts` (Vitest `oracle` project; `import { describe, expect, it } from 'vitest'`). Load the fixture with `readFileSync(fileURLToPath(new URL('../../engine/test/fixtures/base10.golden.json', import.meta.url)))`, assert the raw bytes contain no `\r` and end with `\n`, then `JSON.parse` it as `Base10Oracle`.

Definitions live in the test itself (derived from the numogram definitions, NOT from app/data and NOT from reference/):
```ts
const n = 10
const T = (k: number) => (k * (k + 1)) / 2
const digitalRoot = (t: number) => (t === 0 ? 0 : ((t - 1) % (n - 1)) + 1)
```
Test groups:
1. `oracle equality`: `const { planetary: gp, ...gRest } = golden; const { planetary: dp, ...dRest } = deriveBase10Oracle(); expect(dRest).toEqual(gRest)`; planetary constants (`cx`, `cy`, `radius`, `defaultAngle`, `size`) exact; each `positionsAtDefaultAngle[z].x/y` via `toBeCloseTo(value, 9)`.
2. `gates` (from the golden, checked against T/digitalRoot): the per-k rule; named regressions `it('Gt-15 is 5 -> 6')` and `it('Gt-03 is 2 -> 3')` asserting `{ from, to, cum }` equal `{5, 6, 15}` and `{2, 3, 3}`.
3. `syzygies and currents`: pair sums, coverage, current pair/destination rule, exactly 5 currents.
4. `regions as current cycles`: build the pair->pair map from the golden's pairs (`next(pair) = pair containing |a-b|`), extract cycles, assert the three cycles and the zoneRegion mapping in the behavior block. Regions are modeled as cycles here, never as "the one Torque" (CLAUDE.md).
5. `demons`: count 45, uniqueness, `a > b`, `netSpan === \`${a}::${b}\``, kind classification from `TC = zones of the 3-cycle`, subtype totals (chrono 12 + syzygetic 3 = 15; amphi 12 plex + 12 warp; xeno 4 + syzygetic 2 = 6), `golden.kinds`, `golden.syzygeticBy`, `golden.amphiBy` match those computed counts.
6. `time circuit`: `tc.zones` equals `regions.torque`; `tc.syzygies` sorted equals the 3-cycle pairs; `tc.currents` equals `['Hold','Sink','Surge']`.
7. `planetary`: zone 0 at (400, 400); every position within 1e-9 of the formula.

Run `npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/oracle` and `npx cross-env CCRUG_TZ=America/New_York vitest run --project oracle tests/oracle` (both must pass: the oracle is TZ-independent), then `npm run test`, `npm run test:tz`, `npm run typecheck`. Mutation spot check (never committed): temporarily change the Gt-15 entry's `"to": 6` to `"to": 5` in `engine/test/fixtures/base10.golden.json`, run `npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/oracle` and confirm it FAILS (equality and `Gt-15 is 5 -> 6`), then restore with `git checkout -- engine/test/fixtures/base10.golden.json` and confirm `node scripts/golden-manifest.mjs verify engine/test/fixtures/MANIFEST.json` exits 0. Record the failure output in the SUMMARY. Commit the test file.
  </action>
  <verify>
    <automated>npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/oracle && npx cross-env CCRUG_TZ=America/New_York vitest run --project oracle tests/oracle && node scripts/golden-manifest.mjs verify engine/test/fixtures/MANIFEST.json</automated>
  </verify>
  <acceptance_criteria>
    - `tests/oracle/base10.oracle.test.ts` contains `Gt-15 is 5 -> 6`, `Gt-03 is 2 -> 3`, `((t - 1) % (n - 1)) + 1`, `toBeCloseTo`
    - `tests/oracle/base10.oracle.test.ts` does not contain the string `reference/`
    - The oracle project passes under both CCRUG_TZ values
    - `engine/test/fixtures/base10.golden.json` is byte-identical to its Task 1 commit (`git diff --quiet HEAD -- engine/test/fixtures` exits 0) and its manifest verifies
  </acceptance_criteria>
  <done>The numeric oracle is guarded by an equality test and by definition-derived regressions that hold in two timezones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app/data (upstream lore + numbers) -> frozen fixture | Upstream values become the correctness oracle |
| local reference/ (copyrighted) -> tracked files | Must never cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | base10.golden.json (regenerated to hide a regression) | mitigate | Capture script exits 1 if the file exists; sha256 freeze in `engine/test/fixtures/MANIFEST.json`; definitional assertions do not depend on the JSON matching app/data |
| T-01-07 | Information Disclosure | reference/ book text leaking into fixtures/tests | mitigate | Oracle derived only from `app/data/*`; acceptance greps the test for `reference/`; plan 01-08 guard fails if `reference/` is tracked |
| T-01-07b | Tampering | timezone/locale dependence of the oracle | mitigate | No date input in the derivation; the oracle project runs under CCRUG_TZ=UTC and America/New_York |
</threat_model>

<verification>
- `npm run test` and `npm run test:tz` pass
- `node scripts/golden-manifest.mjs verify engine/test/fixtures/MANIFEST.json` exits 0
- `git diff --quiet 7c38ad9 HEAD -- app` exits 0
</verification>

<success_criteria>
- The numeric half of FND-02 is frozen and tested, including the Gt-15 and Gt-03 regressions and the correct 12+3 / 12+12 / 4+2 demon split
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-safety-net/01-03-SUMMARY.md`
</output>
