---
phase: 01-foundations-and-safety-net
plan: 08
type: execute
wave: 5
depends_on: ["01-06", "01-07"]
files_modified:
  - scripts/check-repo.mjs
  - tests/repo/check-repo.test.ts
  - dist/
  - demo.mov
  - yarn.lock
  - public/.DS_Store
  - .claude/.DS_Store
autonomous: false
requirements: [FND-05, FND-01, FND-02]

must_haves:
  truths:
    - "A guard (`npm run check:repo`) fails if anything under reference/ is tracked, if origin points at lumpenspace/ccru, if dist/, demo.mov, yarn.lock or .DS_Store are tracked, if LICENSE/NOTICE/lore headers are missing, if a frozen golden or fixture changed or contains CR, or if the CI workflow is missing"
    - "origin no longer points at lumpenspace/ccru: the former origin is renamed to upstream (fetch URL https://github.com/lumpenspace/ccru.git, push URL DISABLED_NO_PUSH) and origin is https://github.com/foGledenalzi/CCRUGen.git; no plan task or verify step pushes, fetches or otherwise contacts a remote"
    - "dist/ is untracked but still on disk; demo.mov was deleted by the user before execution and its removal is already committed (history keeps it); yarn.lock and the .DS_Store files are gone"
    - "`npm run verify` passes end to end on a committed tree: guard, typecheck, both TZ test runs, sub-path e2e, build, page-weight, 68 e2e tests, clean tree and static-out checks"
  artifacts:
    - path: "scripts/check-repo.mjs"
      provides: "Repository policy guard with --only, --clean-tree, --static-out"
      exports: ["findTrackedReference", "isUpstreamOrigin", "findTrackedJunk", "missingNoticeEntries", "licenseProblems", "workflowProblems", "gitattributesProblems", "scanStaticOut", "parseDirtyStatus"]
    - path: "tests/repo/check-repo.test.ts"
      provides: "Failure-path tests of every guard helper"
      min_lines: 60
  key_links:
    - from: "scripts/check-repo.mjs"
      to: "scripts/golden-manifest.mjs"
      via: "import { verifyManifestFile }"
      pattern: "verifyManifestFile"
    - from: "package.json scripts.verify"
      to: "scripts/check-repo.mjs"
      via: "npm run check:repo ... && node scripts/check-repo.mjs --clean-tree --static-out"
      pattern: "check-repo.mjs --clean-tree --static-out"
---

<objective>
Close Phase 1: write the repository policy guard (`scripts/check-repo.mjs`, D-04) with tests of its failure paths, then perform the user-approved git mutations LAST (D-10 untrack `dist/`; `demo.mov` was already deleted by the user and its removal committed before this phase; delete `yarn.lock` and the `.DS_Store` files; D-05 as amended: check-then-set the remotes so that `upstream` = lumpenspace/ccru with its push URL disabled and `origin` = `https://github.com/foGledenalzi/CCRUGen.git`, WITHOUT pushing), run `npm run verify` end to end, and ask the user to confirm the licensing defaults.

Purpose: FND-05 ("origin repointed, guard fails if reference/ is ever tracked") and the phase gate for FND-01/FND-02 (clean tree after build, frozen oracles verified). The git mutations are explicitly approved in CONTEXT D-05 (amended) and D-10; history is not rewritten. Pushing is NOT part of this plan: no task or verify step may `git push`, `git fetch`, `git pull`, `git ls-remote` or otherwise contact a remote. Any push is a separate, user-approved step outside all plans.
Output: `scripts/check-repo.mjs`, `tests/repo/check-repo.test.ts`, one hygiene commit, the local remote configuration (no push), a green `verify` run, and user confirmation of LICENSE/NOTICE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/01-foundations-and-safety-net/01-CONTEXT.md
@.planning/phases/01-foundations-and-safety-net/01-RESEARCH.md
@.planning/phases/01-foundations-and-safety-net/01-VALIDATION.md
@CLAUDE.md
@package.json
@scripts/golden-manifest.mjs

<interfaces>
From plan 01-01 `scripts/golden-manifest.mjs`: `export function verifyManifestFile(manifestPath): string[]` (problems; `[]` when intact; `manifest missing: <path>` when absent); `export const ROOT`.
From plan 01-01 `package.json`: `"check:repo": "node scripts/check-repo.mjs"`, `"verify": "npm run check:repo && npm run typecheck && npm run test && npm run test:tz && npm run test:e2e:basepath && npm run build && npm run check:weight && npm run test:e2e && node scripts/check-repo.mjs --clean-tree --static-out"`.
Manifests: `e2e/__golden__/MANIFEST.json` (strictDir `e2e/__golden__`, 30 files), `engine/test/fixtures/MANIFEST.json` (1 file).
Lore header (plan 01-07): `// CCRU-derived lore. Not covered by the MIT license; see NOTICE.` on line 1 of `app/data/{zones,gates,currents,syzygies,demons}.ts`.
CI workflow (plan 01-06): `.github/workflows/ci.yml`.
Git remote state at execution time is EITHER untouched (`origin` = `https://github.com/lumpenspace/ccru.git`, no `upstream`) OR already set up by the orchestrator for the user-approved initial push (`upstream` fetch URL = `https://github.com/lumpenspace/ccru.git`, `upstream` push URL = `DISABLED_NO_PUSH`, `origin` = `https://github.com/foGledenalzi/CCRUGen.git`). Task 2 step 2 is check-then-set and handles both. Current tracked junk = 58 `dist/` files, `demo.mov` (41 MB), `yarn.lock`, `public/.DS_Store`, `.claude/.DS_Store` (62 deletions expected, lab-verified in a clone); `git ls-files reference` is empty.
</interfaces>

Use `execFileSync('git', [...])` (no shell) everywhere in the guard. Run the guard from the repo root (`process.chdir(ROOT)` in the CLI).

Commits: end every commit message with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. NO task or verify step in this plan may run `git push`, `git fetch`, `git pull`, `git ls-remote` or anything else that contacts a remote (D-05 amended): pushing is a separate, user-approved step outside all plans. The only remote change this plan makes is the local, check-then-set remote configuration in Task 2 step 2 (`git remote get-url` and `git remote rename/add/set-url` only touch local config).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repository policy guard with tested failure paths</name>
  <files>scripts/check-repo.mjs, tests/repo/check-repo.test.ts</files>
  <read_first>scripts/golden-manifest.mjs, package.json, .gitattributes, NOTICE, LICENSE, .github/workflows/ci.yml, .planning/phases/01-foundations-and-safety-net/01-RESEARCH.md (section "scripts/check-repo.mjs contract", Validation Architecture table)</read_first>
  <behavior>
    - findTrackedReference(['reference/INDEX.md','app/x.ts','references.md']) === ['reference/INDEX.md']
    - isUpstreamOrigin is true for 'https://github.com/lumpenspace/ccru.git', 'https://github.com/lumpenspace/ccru', 'git@github.com:lumpenspace/ccru.git' (with or without trailing newline) and false for 'https://github.com/foGledenalzi/CCRUGen.git' and 'https://github.com/lumpenspace/ccru-fork.git'
    - findTrackedJunk flags 'dist/a.js', 'demo.mov', 'yarn.lock', '.DS_Store', 'public/.DS_Store' and not 'gematria/plugin/dist/x.js' (untracked by its own ignore) nor 'app/page.tsx'
    - missingNoticeEntries('') lists all five lore files plus 'lumpenspace/ccru'; a text containing all six returns []
    - licenseProblems('') is non-empty; the canonical MIT text returns []
    - workflowProblems('') is non-empty; a workflow containing 'npm run verify', 'ubuntu-latest', 'windows-latest', 'contents: read' returns []; adding 'pull_request_target' yields a problem
    - gitattributesProblems('* text=auto eol=lf\n') === [] and gitattributesProblems('* text=auto\n') is non-empty
    - scanStaticOut on a temp dir: missing index.html is a problem; a file containing 'api/share-image' or '_vercel' is a problem; an 'api' directory is a problem; a '.DS_Store' file is a problem; a clean dir with index.html and numogram/index.html returns []
    - parseDirtyStatus(' M app/x.ts\n?? y\n') returns 2 entries; parseDirtyStatus('') returns []
  </behavior>
  <action>
Write `tests/repo/check-repo.test.ts` first (Vitest `oracle` project, imports from `'../../scripts/check-repo.mjs'`; `scanStaticOut` gets temp dirs from `mkdtempSync(join(tmpdir(), 'ccrug-out-'))`), run red, then implement `scripts/check-repo.mjs` (ESM, zero dependencies):

```js
#!/usr/bin/env node
// Repository policy guard (FND-01, FND-02, FND-05). Exit 1 on any failed check.
//   node scripts/check-repo.mjs [--only a,b] [--clean-tree] [--static-out]
import { execFileSync } from 'node:child_process'
import { existsSync, readdirSync, readFileSync } from 'node:fs'
import path from 'node:path'
import { pathToFileURL } from 'node:url'
import { ROOT, verifyManifestFile } from './golden-manifest.mjs'

export const LORE_FILES = ['app/data/zones.ts', 'app/data/gates.ts', 'app/data/currents.ts', 'app/data/syzygies.ts', 'app/data/demons.ts']
export const LORE_HEADER = '// CCRU-derived lore. Not covered by the MIT license; see NOTICE.'
export const MANIFESTS = ['e2e/__golden__/MANIFEST.json', 'engine/test/fixtures/MANIFEST.json']
export const LF_DIRS = ['e2e/__golden__', 'engine/test/fixtures', 'perf']
export const FORBIDDEN_OUT = [/_vercel/, /vercel-scripts/, /vercel-storage/, /@vercel\//, /api\/share-image/, /num\.qliphoth\.systems/]
```
Pure helpers (exported, exact names): `findTrackedReference(files)`, `isUpstreamOrigin(url)` (regex `/lumpenspace\/ccru(\.git)?\/?$/i` on the trimmed URL), `findTrackedJunk(files)` (`f.startsWith('dist/') || f === 'demo.mov' || f === 'yarn.lock' || f === '.DS_Store' || f.endsWith('/.DS_Store')`), `missingNoticeEntries(text)` (the five LORE_FILES plus `lumpenspace/ccru` not included in the text), `licenseProblems(text)` (requires `MIT License` and `Permission is hereby granted, free of charge`), `workflowProblems(yml)` (requires `npm run verify`, `ubuntu-latest`, `windows-latest`, `contents: read`; forbids `pull_request_target`), `gitattributesProblems(text)` (requires a line equal to `* text=auto eol=lf` after trimming), `scanStaticOut(outDir)` (requires `index.html` and `numogram/index.html`; forbids an `api` directory, any file named `.DS_Store`, and any `.html/.js/.css/.txt/.json/.xml` file whose text matches a FORBIDDEN_OUT pattern; problems name the offending relative path), `parseDirtyStatus(porcelain)` (non-empty lines).

Checks (default set, each returns problem strings; `--only a,b` restricts to named checks):
- `reference`: `findTrackedReference(git ls-files -z)`
- `origin`: `git remote get-url origin` (local config only; never contacts the remote); if the command fails (no origin) -> pass; if `isUpstreamOrigin` -> `origin still points at upstream: <url>`
- `junk`: `findTrackedJunk(git ls-files -z)`
- `license`: LICENSE exists and `licenseProblems` is empty
- `notice`: NOTICE exists and `missingNoticeEntries` is empty
- `lore`: every LORE_FILES file starts with LORE_HEADER
- `goldens`: `MANIFESTS.flatMap(verifyManifestFile)`
- `lf`: no file under LF_DIRS contains byte 13
- `gitattributes`: `.gitattributes` exists and `gitattributesProblems` is empty
- `workflow`: `.github/workflows/ci.yml` exists and `workflowProblems` is empty
Extra checks (only when flagged): `--clean-tree` = `git status --porcelain --untracked-files=no -- . ':(exclude).planning'` must be empty (`.planning/` is excluded: GSD updates STATE.md and ROADMAP.md while the phase runs) (a build/test left a tracked file modified) AND `git status --porcelain --untracked-files=all -- e2e/__golden__ engine/test/fixtures perf` must be empty; `--static-out` = `scanStaticOut(path.join(ROOT, 'out'))`.
Output: each problem as `check-repo: FAIL <check>: <problem>`; success line `check-repo: OK (<comma-separated check names>)`; exit code 1 on any problem. CLI guard: `if (process.argv[1] && import.meta.url === pathToFileURL(path.resolve(process.argv[1])).href)`. `git` via `execFileSync('git', args, { cwd: ROOT, encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] })`.

Then:
1. `npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/repo` passes.
2. `node scripts/check-repo.mjs --only reference,license,notice,lore,goldens,lf,gitattributes,workflow` exits 0 (everything that can already pass).
3. `node scripts/check-repo.mjs` (full default set) exits 1 BEFORE Task 2, listing the tracked junk (`dist/...`, `yarn.lock`, `.DS_Store`; `demo.mov` is no longer tracked), plus `origin still points at upstream` only if the remotes are still untouched. (If the orchestrator already set up the remotes for the user-approved initial push, the `origin` check passes here; its failure path is proven by the `isUpstreamOrigin` unit tests.) Paste the failure summary into the SUMMARY: this is the proof that the guard fails.
4. `npm run typecheck` exits 0. Commit both files.
  </action>
  <verify>
    <automated>npx cross-env CCRUG_TZ=UTC vitest run --project oracle tests/repo && node scripts/check-repo.mjs --only reference,license,notice,lore,goldens,lf,gitattributes,workflow</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-repo.mjs` exports every helper listed in the artifacts and contains `verifyManifestFile`, `--clean-tree`, `--static-out`, `--only`, `execFileSync('git'`
    - `scripts/check-repo.mjs` contains no `execSync(` and no `shell: true`
    - `tests/repo/check-repo.test.ts` covers every behavior bullet with failing inputs (at least 20 `expect(` calls)
    - The partial `--only ...` run exits 0, and the one-time full run BEFORE Task 2 exited 1 naming the tracked junk (and `origin`, if the remotes were still untouched) (output recorded in the SUMMARY; after Task 2 the full run must pass instead)
  </acceptance_criteria>
  <done>A tested guard encodes every Phase 1 repository policy and demonstrably fails on the current (pre-mutation) state for the right reasons.</done>
</task>

<task type="auto">
  <name>Task 2: User-approved git mutations, then the full verify gate</name>
  <files>dist/, yarn.lock, public/.DS_Store, .claude/.DS_Store</files>
  <read_first>.gitignore, package.json, scripts/check-repo.mjs, .planning/phases/01-foundations-and-safety-net/01-CONTEXT.md (D-05, D-10), .planning/phases/01-foundations-and-safety-net/01-RESEARCH.md (section "Answers ... #8 Repo hygiene", Runtime State Inventory)</read_first>
  <action>
These mutations are approved in CONTEXT D-05 (amended) and D-10. History is NOT rewritten (demo.mov stays in old commits; noted as debt in the deferred list). No step here pushes, fetches, pulls, runs `git ls-remote` or otherwise contacts a remote.

1. Make sure the working tree is clean except for the regenerated `dist/` (`git checkout -- dist` first if it is dirty), then run exactly:
```
git rm -r --cached --quiet dist
git rm --quiet yarn.lock public/.DS_Store .claude/.DS_Store
```
Confirm: `git status --porcelain -- . ':(exclude).planning'` shows 61 staged deletions (`D `: 58 dist files, yarn.lock, 2 .DS_Store) and nothing else; `git ls-files demo.mov` prints nothing (already removed before this phase); `test -d dist` (still on disk, now ignored); `yarn.lock`, `public/.DS_Store`, `.claude/.DS_Store` no longer exist on disk. Commit: `chore(01-08): untrack dist/, drop yarn.lock and .DS_Store (D-10)` (with the Co-Authored-By trailer).

2. Remote setup (D-05 amended), check-then-set and idempotent. The orchestrator performs the same setup (rename + add + disabled push URL) before this phase for the user-approved initial push, so it may already be done; in a fresh clone or cloud session `origin` is already the CCRUG URL and `upstream` does not exist yet. Local git config only (repo config, not a commit); never push, fetch, pull or `ls-remote`.
   - If `git remote get-url upstream` fails (no `upstream` yet): if `git remote get-url origin` prints exactly `https://github.com/lumpenspace/ccru.git`, run `git remote rename origin upstream`; else if `origin` is an accepted CCRUG URL (see the next bullet; this is the fresh-clone / cloud-session state), run `git remote add upstream https://github.com/lumpenspace/ccru.git`; anything else: STOP and report.
   - If `git remote get-url origin` now fails (no `origin`): run `git remote add origin https://github.com/foGledenalzi/CCRUGen.git`. If `origin` exists, its URL must be one of two accepted forms: `https://github.com/foGledenalzi/CCRUGen.git`, or `https://foGledenalzi@github.com/foGledenalzi/CCRUGen.git` (the same repo with the username embedded so Git Credential Manager keeps a separate credential for that account; a harmless local variant, leave it as is). Any other URL: STOP and report (never overwrite it).
   - If `git remote get-url --push upstream` does not print exactly `DISABLED_NO_PUSH`: run `git remote set-url --push upstream DISABLED_NO_PUSH` (hardening: a push to `upstream` then fails locally instead of reaching lumpenspace/ccru; the fetch URL is unchanged).
   - If `upstream` already exists and `origin` is an accepted form with the push URL already disabled, change nothing.
   Then assert the FINAL state only: `git remote get-url origin`, with an optional `foGledenalzi@` userinfo removed (`git remote get-url origin | sed 's#//foGledenalzi@#//#'`), prints `https://github.com/foGledenalzi/CCRUGen.git`; `git remote get-url upstream` prints `https://github.com/lumpenspace/ccru.git`; `git remote get-url --push upstream` prints `DISABLED_NO_PUSH`. Record in the SUMMARY which sub-steps ran (or that the state was already final).

3. `node scripts/check-repo.mjs` must now exit 0 (all ten default checks).

4. Phase gate on the committed tree: `npm run verify`. Required: exit 0. Expected stages: check:repo OK; typecheck OK; test and test:tz OK; test:e2e:basepath 8 passed; build OK; check:weight OK; test:e2e 68 passed (60 goldens + 8 static-export) with zero golden diffs; final `check-repo --clean-tree --static-out` OK. Record the wall-clock time per stage in the SUMMARY. If anything fails, fix the cause (never goldens, fixtures or the baseline) and re-run; if the fix needs a commit, re-run `verify` after committing.

5. Confirm afterwards: `git status --porcelain -- . ':(exclude).planning'` is empty; `git ls-files reference | wc -l` prints 0; `git ls-files dist | wc -l` prints 0; `out/.DS_Store` does not exist.
  </action>
  <verify>
    <automated>node scripts/check-repo.mjs && test -z "$(git ls-files dist demo.mov yarn.lock)" && test "$(git remote get-url origin | sed 's#//foGledenalzi@#//#')" = "https://github.com/foGledenalzi/CCRUGen.git" && test "$(git remote get-url upstream)" = "https://github.com/lumpenspace/ccru.git" && test "$(git remote get-url --push upstream)" = "DISABLED_NO_PUSH" && npm run verify && test -z "$(git status --porcelain -- . ':(exclude).planning')"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files dist demo.mov yarn.lock public/.DS_Store .claude/.DS_Store` prints nothing; `dist/` still exists on disk (`demo.mov` was removed by the user and is not tracked)
    - `git remote get-url origin` (optional `foGledenalzi@` userinfo removed) prints exactly `https://github.com/foGledenalzi/CCRUGen.git`; `git remote get-url upstream` prints exactly `https://github.com/lumpenspace/ccru.git` (fetch URL unchanged); `git remote get-url --push upstream` prints exactly `DISABLED_NO_PUSH`
    - No task or verify step ran `git push`, `git fetch`, `git pull` or `git ls-remote` (the SUMMARY lists the git remote commands that were run)
    - `node scripts/check-repo.mjs` exits 0 and prints `check-repo: OK (`
    - `npm run verify` exits 0 on the committed tree; the SUMMARY lists every stage with its result and time
    - `git status --porcelain -- . ':(exclude).planning'` is empty after `verify`; `git ls-files reference` is empty
  </acceptance_criteria>
  <done>The repo is hygienic (junk untracked, npm only), `origin` points at foGledenalzi/CCRUGen while `upstream` (lumpenspace/ccru) cannot be pushed to, nothing was pushed, and the full `verify` gate is green on this machine.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the licensing defaults and Phase 1 decisions (all automation is already done)</name>
  <files>LICENSE, NOTICE (edited only if the user asks)</files>
  <read_first>LICENSE, NOTICE, app/components/numogram/SourcesFooter.tsx</read_first>
  <action>Pause for the user. Nothing is blocked by this checkpoint (verify is already green); it only confirms defaults Claude chose under "Claude's Discretion". If the user supplies a different copyright holder, replace `foGledenalzi` in line 3 of LICENSE and line 2 of NOTICE, commit with the Co-Authored-By trailer (no push), and re-run `node scripts/check-repo.mjs`. If the user asks for NOTICE scope changes, apply them verbatim, keep the five lore file names and `lumpenspace/ccru` present, and re-run the guard.</action>
  <what-built>Phase 1 is complete and `npm run verify` is green. Before closing, confirm the defaults Claude chose where CONTEXT left discretion.</what-built>
  <how-to-verify>
    1. Open `LICENSE`: the copyright line reads `Copyright (c) 2026 foGledenalzi` (the repo's git author, CONTEXT D-02). Is `foGledenalzi` the name you want, or another name?
    2. Open `NOTICE`: it applies MIT only to original post-fork code, leaves every upstream-inherited file un-relicensed (conservative reading of D-01), and excludes the five CCRU lore files. Is that scope right?
    3. Kept as-is by default: the on-page sources footer still says "(c) qliphoth.systems / delight nexus" (credit text, not a URL); the orphan files `app/lib/shareParams.ts`, `app/hooks/useGlitchNavigate.ts`, `app/lib/cyberColors.ts` remain for Phase 4.
    4. Note: the DOM oracle covers 3 layouts (original, labyrinth, ladder) x 10 states (D-14), as ROADMAP success criterion 3 now states; planetary is checked numerically in `base10.golden.json`.
    5. Your parallel actions (not done by Claude): asking lumpenspace for a permissive license; revoking any old Vercel Blob token if one was ever issued to this machine. Remotes are now `origin` = `https://github.com/foGledenalzi/CCRUGen.git` and `upstream` = lumpenspace/ccru (push URL `DISABLED_NO_PUSH`). No plan task pushed anything: the Phase 1 commits reach GitHub, and the CI workflow runs, only when you approve a push, which happens outside the plans.
  </how-to-verify>
  <verify>
    <automated>node scripts/check-repo.mjs --only license,notice</automated>
  </verify>
  <acceptance_criteria>
    - The user replied "approved" or supplied a holder/scope change that was applied and committed
    - `node scripts/check-repo.mjs --only license,notice` exits 0 after any change
  </acceptance_criteria>
  <done>The user has confirmed (or corrected) the LICENSE holder and NOTICE scope.</done>
  <resume-signal>Type "approved", or "holder: NAME", or describe the NOTICE change you want</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree -> git remotes | Commits stay local; only a user-approved push (outside all plans) publishes content |
| local reference/ (copyrighted) -> tracked files | Must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20 | Information Disclosure | reference/ tracked by accident | mitigate | `check-repo` `reference` check (`git ls-files -z` has no `reference/` path) runs first in `verify` and in CI; `.gitignore` keeps `reference/` |
| T-01-21 | Spoofing/Tampering | accidental push to lumpenspace/ccru, or an unapproved push to CCRUGen | mitigate | Former `origin` renamed to `upstream` with push URL `DISABLED_NO_PUSH` (a push there fails locally); new `origin` = `https://github.com/foGledenalzi/CCRUGen.git`; no plan task or verify step runs `git push`, `git fetch`, `git pull` or `git ls-remote` (pushing is a separate user-approved step outside all plans); `check-repo` fails if `origin` points at lumpenspace/ccru again |
| T-01-22 | Denial of Service (data loss) | dist untracking (demo.mov already removed by the user) | mitigate | `git rm --cached` keeps dist/ on disk (verified in acceptance); history untouched |
| T-01-23 | Tampering | guard silently passing | mitigate | Every helper has failing-input tests; the full guard is shown to fail before the mutations and pass after |
| T-01-24 | Tampering | goldens or baseline edited to make verify pass | mitigate | `goldens` and `lf` checks plus `--clean-tree` over `e2e/__golden__`, `engine/test/fixtures`, `perf`; instructions forbid editing them to fix failures |
</threat_model>

<verification>
- `node scripts/check-repo.mjs` exits 0
- `npm run verify` exits 0 on the committed tree; `git status --porcelain -- . ':(exclude).planning'` empty afterwards
- Remote final state: `origin` = `https://github.com/foGledenalzi/CCRUGen.git`, `upstream` fetch = `https://github.com/lumpenspace/ccru.git`, `upstream` push = `DISABLED_NO_PUSH`; nothing was pushed by this plan
- User confirmation of LICENSE/NOTICE recorded
</verification>

<success_criteria>
- FND-05: licensing decided and confirmed, origin repointed (`origin` = foGledenalzi/CCRUGen, `upstream` = lumpenspace/ccru with push disabled, no push), reference/ guard in place and proven
- FND-01/FND-02 gate: build leaves no tracked file modified, frozen oracles verified, CI entry point green locally
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-safety-net/01-08-SUMMARY.md`
</output>
