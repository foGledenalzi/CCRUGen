node_modules/
.next/

# node gitignore
node_modules/
.next/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# playwright gitignore
.playwright-mcp/

# build artifacts
tsconfig.tsbuildinfo
.vercel
.env*.local

# local reference material (copyrighted book PDF/text, scraped web sources, scratch SVGs) - never commit
reference/

# static export and generated artifacts (Phase 1)
/out/
/dist/
/.e2e-basepath/
*.tsbuildinfo

# npm only (yarn.lock is removed in plan 01-08)
yarn.lock

# large demo video: untracked in plan 01-08, kept on disk
demo.mov

# OS junk
.DS_Store

# test output and local browser cache
/test-results/
/playwright-report/
/.pw-browsers/
